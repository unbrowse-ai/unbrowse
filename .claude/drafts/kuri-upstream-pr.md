# Draft PR — lekt9/kuri:feat/windows-port-wave-1 → justrach/kuri:main

Create with:
  cd submodules/kuri && gh pr create --repo justrach/kuri --base main \
    --head lekt9:feat/windows-port-wave-1 \
    --title "Windows port: winsock CDP transport + Chrome discovery + Zig 0.16 ABI" \
    --body-file ../../.claude/drafts/kuri-upstream-pr-body.md

(Outward action to an external maintainer — awaiting Lewis's go. Windows code is
unverifiable here without a Windows host.)
