---
plan: audit-the-unbrowse-capture-enrichment-resolve-ra
plan_text: "Audit the unbrowse capture/enrichment/resolve/rank pipeline and convert every PRESCRIPTIVE heuristic into evidence surfaced to the driving agent's LLM via the MCP. IN SCOPE (remove/surface): per-domain registries and if-host arms, hardcoded confidence switches (computeConfidence type lists), alias tables (q->rawQuery), banned/pattern/refusal-opener lists, hardcoded thresholds that mask judgment, format templates that put words in the agent's mouth, single-segment-only A8 substitution-style guards. OUT OF SCOPE / KEEP DETERMINISTIC (these are evidence-derived generic primitives, not heuristics): BM25 over endpoint text, URL-path keyword overlap, schema richness, host-pattern signal, response-shape bonuses, decomposeGraphqlEndpoint structural decomposition, buildSkillOperationGraph DAG, extractEndpoints generic filters. North star: CLAUDE.md 'eight forbidden surfaces' + 'Ranker philosophy: heuristics OUT, primitives + LLM judge IN' + 'harness collects, agent judges'. Each converted site must surface the raw evidence (samples, schema, candidates, scores) through the existing MCP tool result so the calling agent's LLM judges; never bake a second LLM or a regex verdict into the substrate. Verify per-change with bench-local delta + tsc, no regressions."
project: /Users/lekt9/Projects/unbrowse-ecosystem/unbrowse
template: agent-system
shipping_surface: "cloudflare (wrangler deploy / pages)"
ship_command: |
    git add -A && git diff --cached --quiet || git commit -m 'iterate: $(date +%s)'
verify_gate: "bench-gate + targeted tests"
verify_command: |
    test -f .bench-gate/run.sh && bash .bench-gate/run.sh || echo 'no bench-gate yet; declare verify_command in state file'
loop_primitive: self-build
parallel_budget: 8
iteration_cap: 30
inferred_from:
  template: agent-system
  shipping: meta-harness.local.md
created: 2026-05-19
last_iterated: ""
status: pending
---

# Audit the unbrowse capture/enrichment/resolve/rank pipeline and convert every PRESCRIPTIVE heuristic into evidence surfaced to the driving agent's LLM via the MCP. IN SCOPE (remove/surface): per-domain registries and if-host arms, hardcoded confidence switches (computeConfidence type lists), alias tables (q->rawQuery), banned/pattern/refusal-opener lists, hardcoded thresholds that mask judgment, format templates that put words in the agent's mouth, single-segment-only A8 substitution-style guards. OUT OF SCOPE / KEEP DETERMINISTIC (these are evidence-derived generic primitives, not heuristics): BM25 over endpoint text, URL-path keyword overlap, schema richness, host-pattern signal, response-shape bonuses, decomposeGraphqlEndpoint structural decomposition, buildSkillOperationGraph DAG, extractEndpoints generic filters. North star: CLAUDE.md 'eight forbidden surfaces' + 'Ranker philosophy: heuristics OUT, primitives + LLM judge IN' + 'harness collects, agent judges'. Each converted site must surface the raw evidence (samples, schema, candidates, scores) through the existing MCP tool result so the calling agent's LLM judges; never bake a second LLM or a regex verdict into the substrate. Verify per-change with bench-local delta + tsc, no regressions.

Generated by meta-harness/scripts/builder/build.py on 2026-05-19.

## Layout (skill-creator canon)

```
.claude/audit-the-unbrowse-capture-enrichment-resolve-ra/
├── scripts/
│   ├── verify.sh    runs verify_command + lane bench_commands
│   ├── ship.sh      runs ship_command
│   └── iterate.sh   symlink to shared L0 driver
├── references/
│   └── criteria.md  optional lane declarations
├── ledgers/
│   ├── iterations.jsonl  one row per iterate.sh run (carries log_path + error_excerpt)
│   ├── lanes.jsonl       per-lane bench raw evidence
│   └── gates.jsonl       per-gate evidence (when --gates)
└── logs/
    └── iter<N>-<phase>.log  full stdout+stderr per wave; row.error_excerpt is its stack-trace tail
```

## How to run

```bash
bash .claude/audit-the-unbrowse-capture-enrichment-resolve-ra/scripts/iterate.sh
```

The iterate driver reads this state file, runs scripts/verify.sh, on pass runs
scripts/ship.sh, appends a row to ledgers/iterations.jsonl. Edit the frontmatter
above to change shipping_surface / verify_gate / commands; the scaffold re-reads
on every run.

## Loop primitive

self-build - meaning:

- linear-iterate: one verify -> ship cycle per invocation
- self-build: spawn N parallel probes, delegate to /self-build, write conductor row referencing its run_id
- jesus-loop: delegate to /jesus-loop:take-the-wheel, poll its state via adapter
- evidence-build: delegate to /evidence-build, poll its convergence ledger via adapter

## Optional gate phase

Pass `--gates` to iterate.sh to run the falsifier-borrowed gates between verify and ship:
`bash scripts/iterate.sh --gates --gates-baseline HEAD~5`

## Notes from template

For Claude Code skill/agent/loop builds. Loop primitive 'self-build' fans out N parallel probe sub-agents per wave, judges in-thread, writes wave row. Bench-gate is the verify gate — declare a corpus before iterating.
