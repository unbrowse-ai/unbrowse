# unbrowse-openclaw: E2E env for reverse-engineer backend (docker compose)
#
# This is intentionally "safe dummy" config for local integration tests.
# If you point E2E at a real deployment, prefer E2E_REAL_BACKEND_URL instead
# of changing these values.

USE_PG_VECTOR=true
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/reverse_engineer

BETTER_AUTH_URL=http://localhost:4112
BETTER_AUTH_SECRET=e2e_dummy_secret_please_change

MASTRA_JWT_SECRET=e2e_dummy_mastra_secret_please_change
UNKEY_ROOT_KEY=e2e_dummy_unkey_root_key
UNKEY_API_ID=e2e_dummy_unkey_api_id

BETA_WHITELIST_ENABLED=false
VECTOR_DB_KEEPALIVE_MINUTES=0

# Required by x402-config at import time (must be a valid Solana pubkey).
FDRY_TREASURY_WALLET=8XLmbY1XRiPzeVNRDe9FZWHeCYKZAzvgc1c4EhyKsvEy
