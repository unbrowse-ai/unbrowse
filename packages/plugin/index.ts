/**
 * Unbrowse — OpenClaw extension entrypoint.
 *
 * This file intentionally stays tiny: the real implementation lives under `src/plugin/`
 * so the codebase is easier for agents (and humans) to navigate and iterate on.
 */

export { default } from "./src/plugin/plugin.js";

