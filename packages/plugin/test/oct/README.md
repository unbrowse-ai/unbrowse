# OCT E2E (OpenClaw Test Suite)

These are black-box E2E tests that run against a real `openclaw gateway` process
and invoke tools over HTTP.

Use:
- Local: `bun run test:oct`
- Docker: `bun run test:oct:docker`

