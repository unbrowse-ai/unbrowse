# @getfoundry/unbrowse-core

Unbrowse core library (browser-agnostic): parse captured web traffic into reusable internal API skills.

This package is consumed by:

- OpenClaw extension: `@getfoundry/unbrowse-openclaw`
- Standalone CLI: `@getfoundry/unbrowse-cli`

