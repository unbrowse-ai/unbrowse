## What's New

Resolve documentation now accurately reflects the two-step workflow for multi-endpoint domains. Agents resolving against sites like X, LinkedIn, or Reddit get a clear endpoint selection guide with field-by-field matching criteria (action_kind, dom_extraction, URL patterns). X timeline is used as the primary example throughout.

## Fixes

- Removed misleading --execute one-step claims from Core Workflow and Best Practices sections
- CLI reference tables now split resolve and execute flags separately
- Endpoint selection guide helps agents pick real API endpoints over stale DOM scrapes