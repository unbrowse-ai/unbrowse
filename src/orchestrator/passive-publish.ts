import type { SkillManifest } from "../types/skill.js";

/** Stub — passive skill publishing is not yet implemented. */
export function queuePassiveSkillPublish(_skill: SkillManifest, _opts?: { parity?: unknown }): void {
  // no-op until passive publish pipeline is wired
}
