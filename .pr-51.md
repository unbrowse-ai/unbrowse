## Summary
- Export DEPRECATION_THRESHOLD constant from scoring.ts so callers and tests don't use magic numbers
- Add auto_deprecated_at field to EndpointStats to record when an endpoint was first auto-disabled
- Set auto_deprecated_at timestamp in recordExecution when consecutive failure threshold is hit

## Changes
- backend/src/types.ts: add auto_deprecated_at optional field to EndpointStats
- backend/src/services/scoring.ts: export DEPRECATION_THRESHOLD, use it in recordExecution, set auto_deprecated_at

## Test plan
- [x] 6 tests pass in backend/tests/scoring-deprecation.test.ts

Fixes #51

🤖 Generated with [Claude Code](https://claude.com/claude-code)
