The execute handler called saveTelemetry() locally but never called recordExecution() to POST to the backend /v1/stats/execution endpoint. Backend had no execution telemetry — agent profiles showed zero executions from CLI users. Added fire-and-forget recordExecution() calls after both the primary execution path and the 404-recovery path.
