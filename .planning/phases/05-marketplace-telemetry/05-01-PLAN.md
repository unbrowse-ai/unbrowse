---
phase: 05-marketplace-telemetry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/indexer/index.ts
  - src/orchestrator/passive-publish.ts
  - src/client/index.ts
autonomous: true
requirements: [MARKETPLACE-01]

must_haves:
  truths:
    - "A skill captured by agent A is discoverable by agent B on a different machine via marketplace search within 60 seconds"
    - "An agent resolving an intent with no local skill finds and executes a marketplace skill published by a different agent"
    - "Published skills include operation_graph data so marketplace consumers benefit from graph-aware resolve"
  artifacts:
    - path: "src/indexer/index.ts"
      provides: "Background indexer publishes graph edges alongside skill"
      contains: "operation_graph"
    - path: "src/orchestrator/passive-publish.ts"
      provides: "Passive publish includes graph edges"
      contains: "operation_graph"
    - path: "src/client/index.ts"
      provides: "Client function to publish graph edges and verify discovery"
      contains: "publishGraphEdges"
  key_links:
    - from: "src/indexer/index.ts"
      to: "src/client/index.ts"
      via: "publishGraphEdges call after publishSkill"
      pattern: "publishGraphEdges"
    - from: "src/orchestrator/passive-publish.ts"
      to: "src/client/index.ts"
      via: "publishGraphEdges call after publishSkill"
      pattern: "publishGraphEdges"
---
<objective>
Ensure the skill publish pipeline publishes operation graph data alongside skills and verify cross-agent marketplace discovery works within 60 seconds of capture.

Purpose: Skills captured by one agent must be discoverable and usable by any other agent. The graph structure (Phase 4) must travel with the skill so marketplace consumers get graph-aware resolve.

Output: Modified indexer and passive-publish to include graph edges in publish. Client-side publishGraphEdges function. Verified cross-agent discovery flow.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-marketplace-telemetry/05-RESEARCH.md
@.planning/phases/04-endpoint-graph/04-02-SUMMARY.md
@.planning/phases/02-background-indexing-cache-first/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish graph edges alongside skills in both publish paths</name>
  <files>src/indexer/index.ts, src/orchestrator/passive-publish.ts, src/client/index.ts</files>
  <action>
  Currently both src/indexer/index.ts (line 72) and src/orchestrator/passive-publish.ts (line 70) strip operation_graph before publishing. The graph data needs to travel via the dedicated POST /v1/graph/edges backend endpoint (already exists).

  In src/client/index.ts: Add a new exported async function publishGraphEdges(domain: string, node: { endpoint_id: string; method: string; url_template: string }, edges: Array<{ target_endpoint_id: string; kind: string; confidence: number }>): Promise<void>. If LOCAL_ONLY, return immediately. Otherwise call api("POST", "/v1/graph/edges", { domain, node, edges }) wrapped in try/catch that logs errors but does not throw.

  In src/indexer/index.ts: Import publishGraphEdges from ../client/index.js. After the publishSkill call succeeds (around line 82-83), iterate through skill.operation_graph.operations. For each operation with edges, call publishGraphEdges fire-and-forget with .catch(() => {}). Use job.domain for the domain parameter.

  In src/orchestrator/passive-publish.ts: Import publishGraphEdges from ../client/index.js. After the publishSkill call on line 85, apply the same graph edge publishing pattern. Extract domain from the skill source_url or first endpoint URL using getRegistrableDomain or new URL().hostname.

  Do NOT remove the operation_graph stripping from the skill publish payload -- graphs travel via the dedicated graph/edges endpoint to keep EmergentDB payloads lean.

  Also add publish timing: wrap the publishSkill call in indexer with Date.now() before/after and log: [background-index] publish latency: {ms}ms for {domain}.
  </action>
  <verify>
  bun build src/indexer/index.ts --no-bundle
  bun build src/orchestrator/passive-publish.ts --no-bundle
  bun build src/client/index.ts --no-bundle
  All must compile without errors.
  </verify>
  <done>
  Both publish paths fire graph edge uploads to POST /v1/graph/edges after successful skill publish. Graph data available in backend for cross-agent graph-aware resolve. Skill payload remains lean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add verifyMarketplaceDiscovery diagnostic and validate flow</name>
  <files>src/client/index.ts</files>
  <action>
  Add a new exported async function verifyMarketplaceDiscovery(skillId: string, intent: string, maxWaitMs = 60000): Promise<{ found: boolean; latency_ms: number }> to src/client/index.ts.

  This function polls searchIntent every 2000ms for up to maxWaitMs checking if the given skillId appears in search results. For each result, extract skill_id from metadata (check metadata.skill_id directly, then try JSON.parse(metadata.content).skill_id). Return { found: true, latency_ms } on first match or { found: false, latency_ms } on timeout.

  This is a diagnostic/test helper for verifying Success Criterion 1 (cross-agent discovery within 60s). Not called in production flow.

  Also verify that the existing searchIntentResolve flow in the orchestrator (src/orchestrator/index.ts ~L2900) correctly fetches and uses marketplace skills. Read the resolve flow to confirm that when a marketplace search returns a result with a skill_id, the orchestrator calls getSkill to fetch the full manifest and proceeds with execution. This is already implemented -- just verify it compiles and the imports are correct.
  </action>
  <verify>
  bun build src/client/index.ts --no-bundle -- must compile.
  Verify verifyMarketplaceDiscovery is exported via zigrep.
  </verify>
  <done>
  A verifyMarketplaceDiscovery diagnostic function exists that confirms cross-agent discovery within the 60s window. The existing orchestrator resolve flow correctly fetches and executes marketplace skills.
  </done>
</task>

</tasks>

<verification>
1. bun build src/indexer/index.ts --no-bundle compiles cleanly
2. bun build src/orchestrator/passive-publish.ts --no-bundle compiles cleanly
3. bun build src/client/index.ts --no-bundle compiles cleanly
4. publishGraphEdges is exported from client and called from both indexer and passive-publish
5. Publish latency logging present in indexer
6. verifyMarketplaceDiscovery is exported from client
</verification>

<success_criteria>
- Graph edges published alongside skills via POST /v1/graph/edges
- Both publish paths include graph edge publishing
- Publish timing logged for observability
- Diagnostic function exists to verify cross-agent discovery within 60s
- All modified files compile without errors
</success_criteria>

<output>
After completion, create .planning/phases/05-marketplace-telemetry/05-01-SUMMARY.md
</output>
