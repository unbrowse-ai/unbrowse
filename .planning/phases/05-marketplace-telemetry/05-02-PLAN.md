---
phase: 05-marketplace-telemetry
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/client/index.ts
  - src/telemetry.ts
  - src/orchestrator/index.ts
  - backend/src/services/issues.ts
  - backend/src/types.ts
autonomous: true
requirements: [TELEMETRY-01]
user_setup:
  - service: github
    why: "Auto-filing GitHub issues from agent errors"
    env_vars:
      - name: GITHUB_TOKEN
        source: "GitHub Settings -> Developer Settings -> Personal Access Tokens -> Fine-grained -> Create with 'Issues: Read and Write' permission on unbrowse-ai/unbrowse repo"
    dashboard_config:
      - task: "Add GITHUB_TOKEN secret to Cloudflare Worker"
        location: "Cloudflare Dashboard -> Workers -> unbrowse-api -> Settings -> Variables"

must_haves:
  truths:
    - "When an agent encounters an unhandled error, a GitHub issue is automatically filed without manual action"
    - "Auto-filed issues contain intent, URL, endpoint ID, error message, and kuri version"
    - "Issues are only filed after error threshold is met (3+ errors on same endpoint)"
  artifacts:
    - path: "src/client/index.ts"
      provides: "Client-side autoFileIssue function"
      contains: "autoFileIssue"
    - path: "src/telemetry.ts"
      provides: "Error accumulator for per-endpoint failure tracking"
      contains: "recordFailure"
    - path: "src/orchestrator/index.ts"
      provides: "Wiring that calls error accumulator on failure outcomes"
      contains: "recordFailure"
    - path: "backend/src/services/issues.ts"
      provides: "GitHub API integration for issue creation"
      contains: "fileGitHubIssue"
  key_links:
    - from: "src/orchestrator/index.ts"
      to: "src/telemetry.ts"
      via: "recordFailure call in finalize on failure outcome"
      pattern: "recordFailure"
    - from: "src/telemetry.ts"
      to: "src/client/index.ts"
      via: "autoFileIssue call when threshold met"
      pattern: "autoFileIssue"
    - from: "src/client/index.ts"
      to: "backend/src/routes/issues.ts"
      via: "POST /v1/issues/auto-file API call"
      pattern: "v1/issues/auto-file"
    - from: "backend/src/services/issues.ts"
      to: "GitHub API"
      via: "fileGitHubIssue after createIssue"
      pattern: "api.github.com"
---
<objective>
Wire client-side error accumulation and auto-filing so agent errors automatically create GitHub issues with full reproduction context.

Purpose: Agents hitting bugs in production should automatically file actionable GitHub issues with enough signal to reproduce (intent, URL, endpoint ID, error message, kuri version). No manual developer action required.

Output: Error accumulator in telemetry, client auto-file function, enriched backend issue template with GitHub API integration.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-marketplace-telemetry/05-RESEARCH.md
@.planning/phases/05-marketplace-telemetry/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add error accumulator and client-side auto-file wiring</name>
  <files>src/telemetry.ts, src/client/index.ts, src/orchestrator/index.ts</files>
  <action>
  Backend has POST /v1/issues/auto-file with threshold gating. Missing: client caller + error accumulation.

  In src/telemetry.ts: Add in-memory Map keyed by skill_id:endpoint_id storing error arrays. Export recordFailure() that pushes errors and calls autoFileIssue at threshold 3. Export getAccumulatedErrors() and resetErrorAccumulatorForTests(). Include TRACE_VERSION in each record.

  In src/client/index.ts: Add autoFileIssue() that POSTs to /v1/issues/auto-file. Returns { filed, reason }. Respects LOCAL_ONLY.

  In src/orchestrator/index.ts finalize(): After emitRouteTrace on failure outcome, call recordFailure with skill_id, endpoint_id, trace_id, error, url, intent.
  </action>
  <verify>bun build src/telemetry.ts src/client/index.ts src/orchestrator/index.ts --no-bundle</verify>
  <done>Failed executions accumulate per endpoint. At 3+ errors, autoFileIssue fires. Orchestrator wires failures to accumulator.</done>
</task>
<task type="auto">
  <name>Task 2: Enrich issue template and add GitHub API filing in backend</name>
  <files>backend/src/services/issues.ts, backend/src/types.ts, backend/src/routes/issues.ts</files>
  <action>
  In backend/src/types.ts: Add GITHUB_TOKEN?: string to Env interface.

  In backend/src/services/issues.ts: Add url and kuri_version fields to ReproBundle. Update buildIssueTemplate body to include URL and Kuri version lines. Add buildReproBundle to accept url/kuri_version from errors array.

  Add async function fileGitHubIssue(env: Env, template: IssueTemplate): Promise<{ url: string } | null>. Uses fetch to POST https://api.github.com/repos/{template.repo}/issues with Authorization: token {env.GITHUB_TOKEN}. Returns null if no GITHUB_TOKEN or on failure.

  In backend/src/routes/issues.ts: In the auto-file handler (line 60-95), after createIssue, call fileGitHubIssue via c.executionCtx.waitUntil() for background GitHub filing.
  </action>
  <verify>cd backend && npx tsc --noEmit 2>&1 | head -20</verify>
  <done>Issues include kuri version and URL. Backend files to GitHub via REST API when GITHUB_TOKEN is set. Auto-filed issues appear on GitHub with full repro context.</done>
</task>
</tasks>

<verification>
1. bun build src/telemetry.ts --no-bundle compiles cleanly
2. bun build src/client/index.ts --no-bundle compiles cleanly
3. bun build src/orchestrator/index.ts --no-bundle compiles cleanly
4. Backend TypeScript compiles cleanly
5. recordFailure exported from telemetry, called from orchestrator
6. autoFileIssue exported from client, called from telemetry
7. fileGitHubIssue exported from backend services/issues.ts
8. GITHUB_TOKEN in backend Env interface
</verification>

<success_criteria>
- Agent failures accumulate per endpoint and auto-file after 3+ errors
- Backend creates GitHub issues via REST API with full repro context
- Issue template includes: intent, URL, endpoint ID, error, kuri version, traces
- All modified files compile without errors
</success_criteria>

<output>
After completion, create .planning/phases/05-marketplace-telemetry/05-02-SUMMARY.md
</output>
