---
phase: 01
plan: 01
title: P1 Seed — re-export rankEndpoints from src/ranking/
wave: 1
depends_on: []
files_modified:
  - src/ranking/index.ts
  - tests/ranking-seed.test.ts
  - PAPER_PLAN.md
  - CHANGELOG.md
autonomous: true
requirements:
  - PAPER_PLAN-P1
---

# Plan 01-01: P1 Seed — re-export rankEndpoints

## Objective

Plant the smallest viable artifact for PAPER_PLAN.md §P1 (Unified Ranking
State Machine): a re-export shim at `src/ranking/index.ts` that exposes
`rankEndpoints` and `RankedEndpoint` to future call-site migrations
WITHOUT changing any scoring behavior. Behavior consolidation is Waves 2–4.

## must_haves

- [ ] `src/ranking/index.ts` exists and re-exports `rankEndpoints` + type `RankedEndpoint` from `src/execution/index.ts`.
- [ ] `tests/ranking-seed.test.ts` asserts the import path resolves and the function is callable.
- [ ] `PAPER_PLAN.md` orphan ±15 % honesty-contingency block (lines 314–316) deleted.
- [ ] `CHANGELOG.md` Unreleased > Features carries a `**ranking:**` entry naming this seed.
- [ ] `bun test tests/ranking-seed.test.ts` is green.
- [ ] No edit to `src/execution/index.ts` in this wave.

## Tasks

1. Create `src/ranking/index.ts` with re-export only. Doc-comment names the migration plan and points at `01-01-PLAN.md`.
2. Create `tests/ranking-seed.test.ts` (≤30 lines, bun:test, three assertions: import resolves, function callable, empty-input invariant).
3. Edit `PAPER_PLAN.md` to delete duplicate ±15 % paragraph at lines 314–316.
4. Append CHANGELOG entry under Unreleased > Features.
5. Run `bun test tests/ranking-seed.test.ts tests/paper-plan.test.ts` and confirm both green.

## Wave Map (entire Phase 01 across loops)

| Wave | Plan | Goal | Owns |
|---|---|---|---|
| 1 | 01-01 (this) | re-export shim + drift cleanup | `src/ranking/index.ts`, seed test |
| 2 | 01-02 (next loop) | discover & migrate every rank call site to import from `src/ranking/` | imports across `src/cli.ts`, `src/api/routes.ts`, `src/orchestrator/`, etc. |
| 3 | 01-03 | extract each numeric delta into `src/ranking/signals/*.ts` (sim, reliability, freshness, verification, intent-yield, hard-clamp, density) | `src/ranking/signals/`, integrate `intent-yield.ts` + `semantic-enrich.ts` |
| 4 | 01-04 | regression fixtures for the 6 named fixes; `grep -nE "score \\+= [0-9]" src/` returns zero outside `src/ranking/` | `tests/ranking-regressions.test.ts`, prune `src/execution/index.ts` |

## Anti-Goals (this wave)

- Do NOT modify `src/execution/index.ts` scoring logic.
- Do NOT introduce new scoring weights (P2 owns those).
- Do NOT add per-domain heuristics anywhere.
- Do NOT publish a `--force-publish` style escape hatch.
- Do NOT delete the existing `rankEndpoints` function until Wave 4.

## Done-when (Wave 1)

- All `must_haves` checkboxes flipped.
- `bun test tests/ranking-seed.test.ts` → green.
- `bun test tests/paper-plan.test.ts` → still 54/0 after PAPER_PLAN.md edit.
- §"Status of Each Paper Claim" row 9 stays `missing` (this is a seed, not P1 completion); evidence column updated to mention the seed.
