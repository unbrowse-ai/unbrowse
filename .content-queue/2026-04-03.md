# 2026-04-03 GTM Queue

## Status

- Public stats truth verified live this morning.
- `@unbrowse` already used paper-drop, browser-tax, speedup, and cost-reduction hooks.
- Fresh angle for today: network scale + route reuse + live public proof.

## X Draft

Hook:

103 agents have already mapped 160 domains into 10,997 callable routes.

That is the real moat for agent web tooling.

Thread:

1. 103 agents have already mapped 160 domains into 10,997 callable routes. That is the real moat for agent web tooling.
2. The first browser run should not die as exhaust. It should become reusable infrastructure.
3. Unbrowse watches the traffic from real browsing, learns the internal API routes, then replays them directly next time.
4. Public network summary right now: 239 skills, 10,997 endpoints, 722 executions, 103 agents.
5. 75% of resolves are already hitting cache or marketplace instead of paying full browser cost.
6. On the benchmark side: 94 domains, 3.6x mean speedup, 5.4x median, 100% win rate vs Playwright.
7. Repo: 612 stars. npm: 4,776 monthly downloads. Paper: https://arxiv.org/abs/2604.00694
8. If your agent revisits the same sites, the question is not "which browser?" It's "why are we still re-rendering known routes?"

Media:

1. benchmark visual
2. route-learning / shared-network visual
3. product CLI or dashboard proof

## Show HN Package

Title:

`Show HN: Unbrowse — a browser for agents that turns websites into reusable APIs`

First comment:

Built this because browser agents keep rediscovering the same websites over and over.

Unbrowse takes the first browser session, captures the traffic, learns the internal API routes, and turns that discovery into a reusable skill for the next run.

Current live network numbers:

- 239 skills
- 10,997 endpoints
- 160 domains
- 722 executions
- 103 agents
- 75% cache-or-marketplace hit rate

Benchmark numbers:

- 94 domains
- 3.6x mean speedup vs Playwright
- 5.4x median
- 100% win rate on the benchmark set

Paper: https://arxiv.org/abs/2604.00694

Happy to answer where this breaks too: WAF churn, GraphQL POST complexity, and long-tail cold starts are all real.

## Investor Update

Subject:

Unbrowse public traction update

Body:

Quick public update:

- GitHub repo now at 612 stars
- npm at 4,776 monthly downloads
- live network summary at 239 skills / 10,997 endpoints / 160 domains / 103 agents
- public traction surface at 99,768 verifications / 195 WAU / 831 total keys / 45% weekly retention
- paper live on arXiv

We now have enough public proof to push a coordinated HN + X launch without hand-waving the numbers.

## Notes

- Do not use x-cli to post. Queue in Typefully.
- Avoid repeating the existing paper / benchmark-only hook on `@unbrowse`.
- If using `@getFoundry`, this draft is clean because that account appears inactive.
