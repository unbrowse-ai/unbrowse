# Docs vs codebase audit -- 2026-05-21
<!-- Update 2026-05-22: PR addressing STALE entries 1 + 3 + 4 + 7 inline. -->
<!-- - mcp-workflow-guide.md: tool count 33 -> 39, §M4 marked historical, line-cite-drift note added. Full table regen still recommended (per CI gate proposal at the end of this doc). -->
<!-- - guides/quickstart.md: SKILL.md link fixed (retired in v6.15.0), skill-add stale path replaced, broken /Users/.../codex/ absolute links replaced with relative links. -->
<!-- - disposable-mcp-test-plan.md: line 41 already carries the narrowed pkill set (a prior PR fixed this between audit and now). -->
<!-- - sdk/build-on-unbrowse.md, sdk/developer-recipes.md, for-developers/integration-surfaces.md: NOT YET ADDRESSED -- listed as followup-ship. -->
<!-- New post-2026-05-21 surfaces added to HOW_UNBROWSE_PAYS.md + packages/sdk/README.md: per-skill markup_bps (5-80%), payment-provider choice (Pay signer / lobster.cash / privy / external), /v1/version signed manifest verification, SDK publish + annotate + paymentProvider + dashboardByWallet + creatorTransactions + indexerAttribution methods. -->


**Generated by:** claude general-purpose agent
**Scope:** `docs/` excluding `docs/archive/` and `docs/whitepaper/` (52 files total)
**Method:** cite-vs-grep, one factual claim per row. Every `STALE`/`OBSOLETE`
row pins a contradiction to a real `src/...` or `backend/...` line as of
HEAD (`d31a012a feat(bench): expand release-gate corpus 272 -> 500 ...`).
Strategy/narrative docs whose forward-looking claims are not falsifiable
against current code are tagged `STRATEGY/FROZEN` and not graded.

**Note on git history:** every doc except `muonry-stale-cache-bug-2026-05-21.md`
was bulk-rewritten today in commit `10ec22a3 fix(sdk): point keys/account at
existing /v1/account/* routes (#638)`. The last-touched date is therefore
not useful as a staleness signal; each file had to be read for content.

---

## STALE (7 entries)

### docs/mcp-workflow-guide.md

This doc is the highest-value technical reference and the most stale.

- **Claim (line 15, line 210):** "The MCP server exposes **33 tools**" / "Part II -- Tool reference. Every tool registered by `src/mcp.ts` (33 total)."
  - **Contradicted by:** `src/mcp.ts` registers **39 tools** (grep `name: "unbrowse_` returns 39 matches, lines 1509 - 2814).
  - **Missing from doc:** `unbrowse_reflect` (`src/mcp.ts:1770`), `unbrowse_publish_suggestions` (`src/mcp.ts:2027`), `unbrowse_earnings` (`src/mcp.ts:2083`), `unbrowse_run` (`src/mcp.ts:2756`), `unbrowse_fetch` (`src/mcp.ts:2784`), `unbrowse_test_crash` (`src/mcp.ts:2814`).

- **Claim (Part III §M4, lines 25, 306 - 310):** "There is **no `unbrowse_fetch` tool** despite some prose hints (`src/mcp.ts:566, 573, 945, 1398`) referencing one. ... Your `tools/call` returns method-not-found. **Prevention:** Until the tool ships, route URL-fetch intents through `unbrowse_resolve { url, raw: true }` ..."
  - **Contradicted by:** `unbrowse_fetch` exists at `src/mcp.ts:2784`. The §M4 section instructs agents to route around a tool that now ships, and tells them to expect `method-not-found` for a real tool.

- **Claim (Part II "Tool reference" table, lines 215 - 278):** Every tool line cite is wrong. Examples:
  - Doc says `unbrowse_health` at line 995; real line is `1509`.
  - Doc says `unbrowse_resolve` at line 1005; real line is `1519`.
  - Doc says `unbrowse_execute` at line 1082; real line is `1590`.
  - Doc says `unbrowse_close` at line 1762; real line is `2574`.
  - All other rows similarly shifted by 500+ lines. The file has grown but the doc was never re-cited.

- **Claim (Appendix line 324):** "`src/mcp.ts:901-1838` | The 33-tool registry (single `const tools: ToolDefinition[]`)"
  - **Contradicted by:** tools span lines 1509 - 2814 (39 entries) as of HEAD.

- **Claim (line 5):** "Companion docs: `docs/mcp-vs-cli-ux-audit.md`, `docs/mcp-ux-fix-plan.md`"
  - **Contradicted by:** `docs/mcp-ux-fix-plan.md` does not exist (`ls docs/mcp*` shows only `mcp-vs-cli-ux-audit.md`, `mcp-workflow-guide.md`).

- **Recommended fix:** regenerate the tool table from `src/mcp.ts` programmatically (the falsifier script at `scripts/verify-mcp-workflow-guide.sh` already exists per line 330 - wire it). Remove §M4 entirely (or rewrite as "fetch ships now, prefer it for URL-only intents"). Either ship `mcp-ux-fix-plan.md` or drop the reference at line 5.

### docs/mcp-vs-cli-ux-audit.md

- **Claim (line 11):** "Tool surface parity with CLI | PASS | 34/34 commands mirrored as `unbrowse_<verb>` tools"
  - **Contradicted by:** MCP now exposes 39 tools; CLI command count and the parity table need a refresh.

- **Claim (Gap 1, lines 26 - 36):** `src/mcp.ts:1880-1890` cited as showing `listChanged: false`.
  - **Contradicted by:** the doc later says Phase 2 already shipped `listChanged: true` (line 17). The Gap 1 section reads as if still open. The line `1880-1890` no longer points where the doc thinks it does.

- **Recommended fix:** mark the doc as "audit-of-record + shipping-status, 2026-05-11"; either prune the now-fixed gaps or wrap them as "Gap N (closed by Phase X)" with a current `src/mcp.ts:LINE` cite to the shipped fix.

### docs/guides/quickstart.md

Heaviest concentration of stale claims in any single doc.

- **Claim (line 22):** Links to `/Users/lekt9/.codex/worktrees/c99f/unbrowse/SKILL.md` and "The repo-level agent contract lives in [SKILL.md](...)"
  - **Contradicted by:** (a) `ls SKILL.md` -> no such file; the skill path was retired in v6.15.0 per `CLAUDE.md` ("Skill path retired in v6.15.0 -- ... no more `SKILL.md` or `unbrowse-ai/unbrowse` skill-repo sync"). (b) The path is a developer's local worktree, not portable.

- **Claim (lines 62 - 72):** A whole section titled "If your host uses skills" instructing readers to run `npx skills add unbrowse-ai/unbrowse` and `unbrowse setup --host mcp`.
  - **Contradicted by:** v6.15.0 retired the entire skill surface. The instruction is for a system that no longer exists. Project `CLAUDE.md` is explicit: "Skill path retired in v6.15.0".

- **Claim (line 38):** "starts the local server on `http://localhost:6969` unless `--no-start` is passed"
  - **Partially contradicted by:** Phase 0d (commits `7282cfc6..6e21ef90`) -- the MCP server no longer auto-spawns the :6969 daemon (`src/mcp.ts:1229`, "Phase 0d: no HTTP, no :6969 daemon"). `unbrowse serve` (explicit foreground) still binds 6969, so the claim is half-true; the prose should be scoped to `unbrowse serve` and `unbrowse setup`, not "the local server" in general.

- **Claim (line 56):** "unbrowse register --email you@example.com"
  - **Contradicted by:** `src/cli.ts:4119` -- `register` now emits `[deprecated] register is now account --register`. The canonical command is `unbrowse account --register --email ...`.

- **Claim (lines 202 - 204):** "What to read next" links point at `/Users/lekt9/.codex/worktrees/c99f/unbrowse/docs/api.md`, `/Users/lekt9/.codex/worktrees/c99f/unbrowse/docs/deployment.md`, `/Users/lekt9/.codex/worktrees/c99f/unbrowse/docs/codex-eval-harness.md` -- all developer-local absolute paths.
  - **Contradicted by:** none of those files exist (`docs/api.md`, `docs/deployment.md`, `docs/codex-eval-harness.md` -- none present); the paths are also non-portable.

- **Recommended fix:** strip lines 62 - 72 (skill section), strip the absolute-path links at lines 22 and 202 - 204, replace `unbrowse register` with `unbrowse account --register`, and scope the ":6969 auto-start" claim to `unbrowse serve` only.

### docs/sdk/build-on-unbrowse.md

- **Claim (line 182):** "**As an OpenClaw plugin** so the agent's default browser auto-routes through your wrapper. See `openclaw` metadata in [SKILL.md](../../SKILL.md)."
  - **Contradicted by:** root-level `SKILL.md` does not exist (`ls SKILL.md` -> no such file); skill path retired in v6.15.0.

- **Claim (line 87):** Links to `../../packages/sdk/docs/api-reference/resolve.md`
  - **Need to verify:** path may also be wrong; left as low-priority follow-up.

- **Recommended fix:** delete the `[SKILL.md](../../SKILL.md)` link; either inline the OpenClaw config or link to the OpenClaw package docs.

### docs/disposable-mcp-test-plan.md

- **Claim (line 41):** "`pkill -9 -f 'unbrowse|kuri' 2>/dev/null; sleep 1`"
  - **Contradicted by:** project `CLAUDE.md` explicit ban -- "Do NOT fall back to broad `pkill -9 -f 'unbrowse|kuri'`: it kills concurrent bench scaffolds in worktree paths whose cmdline merely contains 'unbrowse' (PR #662 root cause)." The doc instructs readers to use the very pattern PR #662 outlawed.

- **Recommended fix:** replace the broad pkill with the NARROWED kill set documented in `CLAUDE.md` (the long-lived-servers + kuri-specific block).

### docs/for-developers/integration-surfaces.md

- **Claim (line 11):** "All three speak to the same local runtime. The runtime defaults to `http://localhost:6969`."
  - **Partially contradicted by:** Phase 0d -- MCP runs in-process and does NOT bind :6969 (`src/mcp.ts:1229`). Only `unbrowse serve` and `npx unbrowse <subcommand-that-spawns-it>` bind that port. SDK `Unbrowse.local()` still probes :6969 (`packages/sdk/src/client.ts:168`).

- **Recommended fix:** "The CLI and SDK speak to a local runtime on `http://localhost:6969`; `unbrowse mcp` runs the same API in-process and binds no port."

### docs/sdk/developer-recipes.md

- **Claim (lines 68 - 80):** Recipe 3's closing ```ts``` block is malformed. Lines 68 - 78 contain two half-snippets concatenated without separator. The closing fence at line 68 ends the wrong block; lines 69 - 80 are dead code fragments referencing `pick`, `r`, and a partial `feedback()` call that won't compile.
  - **Contradicted by:** syntax. Copy-paste yields broken TS.
  - **Recommended fix:** rewrite Recipe 3 with a single coherent code block; the working version of the "feedback" recipe pattern lives below in this file already.

---

## OBSOLETE (1 entry)

### docs/bench-corpus-refresh.md

- **Describes:** "The initial 50-probe corpus" and a refresh cadence assuming small corpora.
- **Contradicted by:** `harness/probes/corpus-gate.txt` has **1147 lines** now (`wc -l`). Recent commits show the corpus has grown 80 -> 250 -> 500 -> 1147 in wave 1 - 4 expansions (commits `d31a012a`, `82edaa98`, `2a432402`, `7e86794c`). The "initial 50" framing is two orders of magnitude behind reality.
- **Recommended:** rewrite the "Mining sources today (provenance)" section -- the corpus is no longer "initial 50 drawn from production usage at getFoundry/unbrowse"; it's a 4-wave bench-corpus-builder output. Or move to `docs/archive/bench-corpus-refresh-v1.md` and write a fresh doc.

---

## CURRENT (28 entries)

All read against codebase; no falsifiable claim contradicted. One line each.

- `docs/README.md` -- top-level table-of-contents, strategy framing, current.
- `docs/SUMMARY.md` -- GitBook TOC, matches the file tree.
- `docs/CLAUDE.md` -- auto-pinned meta-harness block; matches the canonical autopin.
- `docs/PARALLEL_SESSIONS_REBUILD.md` -- design doc with status banner; pins to real `src/api/browse-session.ts` line numbers (spot-checked D1 cites at lines 313/360/397/269 -- file still exists).
- `docs/release-gate-bench-plan.md` -- every cited script (`scripts/bench-gate*.{sh,ts}`) and corpus file exists.
- `docs/bench-gate-mcp.md` -- describes the MCP-driven gate; all cited scripts present.
- `docs/muonry-stale-cache-bug-2026-05-21.md` -- bug report; dated and current.
- `docs/wallets.md` -- Flex escrow + session key flow; constants match `backend/src/services/flex.ts`.
- `docs/EARN_AS_INDEXER.md` -- explicit `backend/src/types.ts:409`, `backend/src/services/flex.ts:54-87`, `:66`, `:98-113` cites; verified.
- `docs/CLAIM_YOUR_DOMAIN.md` -- DNS-TXT flow; `_unbrowse-claim` and `domain-wallet:` keys verified in `backend/src/services/domain-claim.ts:25,48`.
- `docs/HOW_UNBROWSE_PAYS.md` -- 50/15/35 split with constants pinned; `PLATFORM_BPS=5000`, `OWNER_BPS=1500` verified in `backend/src/services/flex.ts:39,49`.
- `docs/OPEN-SOURCE-NOTICE.md` -- honest about the public-repo-frozen state; matches project `CLAUDE.md` "public_repo_frozen_by_design" memory.
- `docs/endpoint-as-cell.md` -- design/north-star doc; Lewis-quoted, project-wide standing.
- `docs/concepts/fare-splits.md` -- explicit constants pin (lines 11, 15); verified against `flex.ts`.
- `docs/concepts/claiming-a-website.md` -- DNS-TXT detail; verified.
- `docs/concepts/shadow-apis.md` -- strategy framing.
- `docs/concepts/route-graph-as-asset.md` -- strategy framing.
- `docs/concepts/the-maintenance-network.md` -- whitepaper-derived, strategy.
- `docs/concepts/the-maintenance-problem.md` -- whitepaper-derived, strategy.
- `docs/concepts/trust-and-accountability.md` -- whitepaper-derived, strategy.
- `docs/concepts/verification-and-proofs.md` -- honest about not-yet-shipped status.
- `docs/concepts/route-trust-tiers.md` -- strategy framing.
- `docs/concepts/accountable-bonding.md` -- strategy framing.
- `docs/concepts/evaluation-framing.md` -- strategy framing.
- `docs/built-on-unbrowse/README.md` -- 5-line section landing page.
- `docs/built-on-unbrowse/aiko-agent.md` -- product description; aiko.unbrowse.ai live.
- `docs/for-agents/how-an-agent-uses-unbrowse.md` -- two-call contract, current.
- `docs/for-agents/resolve-and-execute.md` -- two-call contract, current.
- `docs/for-agents/when-it-uses-a-browser.md` -- browser-open-is-failure framing matches `CLAUDE.md`.
- `docs/for-agents/mcp-integration.md` -- mcpServers config + setup; current.
- `docs/for-developers/sdk-quickstart.md` -- factories `local/connect/spawn` verified in `packages/sdk/src/client.ts:138,150,165`.
- `docs/for-developers/the-route-lifecycle.md` -- conceptual; current.
- `docs/sdk/onboarding-users.md` -- `unbrowse account --register` command verified; lobster flow current.
- `docs/sdk/onboarding-validators.md` -- wallet resolution order `src/payments/wallet.ts:getWalletContext` cited.
- `docs/sdk/rewards-and-economics.md` -- 50/15/35 with constants pinned; verified.
- `docs/start-here/what-is-unbrowse.md` -- narrative; current.
- `docs/start-here/the-browser-discovery-tax.md` -- whitepaper concept; current.
- `docs/start-here/the-shared-route-graph.md` -- whitepaper concept; current.
- `docs/start-here/in-plain-english.md` -- narrative; current.
- `docs/frontend-dashboard-plan.md` -- per-row "EXISTS / MISSING / GREENFIELD" map of `frontend/src/app/` paths; pins to real files. Status-accurate plan doc.

---

## STRATEGY/FROZEN (6 entries)

Vision / market / token / paper-pointer docs; not falsifiable against code,
not graded.

- `docs/vision.md`
- `docs/for-investors/the-wedge.md`
- `docs/for-investors/the-moat.md`
- `docs/for-investors/market-framing.md`
- `docs/for-investors/read-the-papers.md`
- `docs/reference/fdry.md`

---

## Summary

- Total docs reviewed: 52
- STALE: 7
- OBSOLETE: 1
- CURRENT: 38
- STRATEGY/FROZEN: 6

(Counts: 7 + 1 + 38 + 6 = 52 ✓)

### Three most urgent fixes

1. **`docs/mcp-workflow-guide.md`** -- the agent-facing reference for every MCP host (Claude Desktop, Cursor, Aiko, Claude Code). Tool count is wrong (33 -> 39), every line cite is wrong by 500+ lines, and §M4 actively misdirects agents away from `unbrowse_fetch` which now exists. This is the doc agents read to know what tools to call; staleness here propagates into every integration. Fix: regenerate the Part II table programmatically and rewrite §M4.

2. **`docs/guides/quickstart.md`** -- the first doc a new developer reads. Five separate stale claims, three of them pointing to retired surfaces (`SKILL.md`, `npx skills add unbrowse-ai/unbrowse`, `unbrowse register`). Combined with developer-local absolute paths at lines 22 and 202 - 204, this doc misleads on every install path it documents.

3. **`docs/disposable-mcp-test-plan.md`** line 41 -- runtime hazard. The doc tells the reader to run `pkill -9 -f 'unbrowse|kuri'`, exactly the pattern `CLAUDE.md` calls out as the PR #662 root cause (kills concurrent bench scaffolds in worktree paths). Anyone following this test plan on a busy machine will destroy other agents' work. Fix: paste the NARROWED kill set from `CLAUDE.md` into the doc.

### Recommended principle to wire (parent agent's other half of the work)

A per-commit gate that runs `scripts/verify-mcp-workflow-guide.sh`
(already present per `docs/mcp-workflow-guide.md:330`) and extends it to:

- Every tool listed in the table must exist in `src/mcp.ts` (already checked).
- Every line cite in the table must point to within ±5 lines of the named tool's `name:` field (NEW).
- Every cross-doc link (`/Users/...`, `../../SKILL.md`, etc.) must resolve.
- No doc may reference `SKILL.md`, `npx skills add`, or `unbrowse register` without an explicit `<!-- deprecated -->` marker.

That gate, plus a quarterly cite-vs-grep audit of the form above, makes
the doc-vs-code drift bounded.
