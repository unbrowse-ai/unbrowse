# Paper ⟂ Code gap ledger — "what we say vs what we do"

**Question:** what are the published papers claiming as shipped that the live code does NOT actually do right now?

**Key framing (verified):** `paper-gate.sh` is GREEN and `\impl{}` count is 0 — every claim *resolves to a real
code anchor* (`anchors.tsv`). So the gaps are NOT missing code. They are the subtler, truer kind: **the anchor
exists, but the behavior behind it is hollow** — empty data, devnet-only, flag-off, or moat-unreachable. The old
"shipped / not-shipped" binary can't name this; the new skins (MOAT-ONLY, DATA-EMPTY) can.

> SEED (Step 3). Verified-now gaps below. The audit worker's fuller per-artifact enumeration extends this.

| # | Paper claim (the WHAT) | Capability | Verdict | Evidence (verified this run) |
|---|---|---|---|---|
| 1 | The route graph / marketplace **index** is the moat agents resolve against | index | **DATA-EMPTY** | `skills-v2:bm25-idx:v2-global` AND `staging-skills-v3:bm25-idx:stg2-global` both `found=False` in EmergentDB — the index is live but holds **zero routes**, prod + staging |
| 2 | Indexing happens **passively from usage** (capture→publish grows the graph) | capture/index | **MOAT-ONLY** | `autoIndexFromReveng` shipped (3a4a6ce0, live on staging) but `/reveng` is behind `requireSignedClient` (auth.ts:255) → no unverified client can feed it; index stays empty (#1) |
| 3 | **x402 / FLEX owner-credit settlement** — websites own wallets, per-use payment settles | payments | **DEVNET-ONLY** | `scripts/flex-devnet-settle.mjs`: "against the deployed FLEX program (devnet-only)" — no mainnet settlement path live |
| 4 | **DAG recompute at runtime** / composite persist + replay (the north-star runtime DAG) | DAG/cache | **FLAG-OFF** | gated behind `UNBROWSE_LOCAL_CACHES=1`, default OFF (`src/...:68-69`) — off in shipped runtime |
| 5 | **Research / groundedness parity** (beat Exa on attribution) | research | **PARTIAL** | native research shipped, but groundedness ~0.5 vs Exa 0.79 — reader-bound below the claimed parity (capability-bench ceiling) |

## Top say-vs-do gaps (impact-ranked)
1. **The index is empty (DATA-EMPTY).** The single most load-bearing claim — "agents resolve against a maintained
   route graph" — is hollow in practice: `bm25-idx` is empty on prod and staging. Resolve falls back to web search.
2. **Passive indexing is unreachable (MOAT-ONLY).** Just shipped, but the moat that protects it also starves it —
   only release-verified clients can post captures, so the graph can't grow from ordinary usage yet.
3. **x402/FLEX settlement is devnet-only.** The economic claim (websites-own-wallets, per-use settlement) has no
   mainnet path — the round-trip is proven only on devnet.
4. **Runtime DAG recompute is flag-off.** The north-star "DAG recompute at runtime" ships behind a default-off env flag.
5. **Research groundedness is below the parity it implies.** Shipped but reader-bound under the Exa attribution bar.

## Witness
- Mechanical: `scripts/paper-gate.sh paper/crypto-was-all-you-needed.tex` → **PASS** (58 anchors, 0 leak) — anchors exist.
- Behavior: the 5 rows above — each verdict backed by a real grep / live qdkv check. **No fabricated "fully reflects code."**

## Open (worker extends)
- Per-artifact enumeration of the trilogy + 2 public PDFs + README (claims beyond the gated `.tex`).
- Confirm which claims are PUBLISHED-public vs dev-repo/internal-only (audit only the public ones).
