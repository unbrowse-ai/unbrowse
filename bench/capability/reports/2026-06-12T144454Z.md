# unbrowse capability bench - 2026-06-12T144454Z

**Headline:** post-fetch-ladder-lever

## Gate verdicts (runnable witnesses)

| gate | exit | meaning |
|------|------|---------|
| `gate_all.sh` | 0 | PASS |
| `gate_real.sh` | 0 | PASS |
| `gate_selfimprove.sh` | 0 | PASS |

## Axis A - action-retrieval / indexing coverage

- overall coverage@1 = **0.7778** (+0.0000 vs prev), correct@1 = 0.7778, n = 9
  - tier **R**: coverage 1.0 (n=3)  [Reddit]
  - tier **H**: coverage 0.6667 (n=3)  [hardest-scrape]
  - tier **A**: coverage 0.6667 (n=3)  [automation]
- **what regressed / gaps:** 2 miss(es): H2(H), A2(A)
  - **why:** retrieval discovers endpoints behind a PAGE; misses are typically SPA/anti-bot pages whose XHR endpoint was not surfaced, or a blocked capture. These are the self-improvement target, not hidden.

## Axis B - execution (two-witness, content-bound)

- PASS: key=rust expect_ok=True gate=true
- **what works:** two distinct sessions content-bound to the requested resource.

## Axis C - execution with auth

- PASS: cookies_injected=5 session_stateful=True logged_in=False authed=True
- **what works:** cookie-injected execution returns a session-stateful response.

## Axis D - security leak-scan

- PASS: session_files_scanned=0 leaks=0 leak_clean=True
- **what works:** persisted session files are pointer-only (redaction invariant holds).


## Real cloned benchmark (github:exa-labs/benchmarks)

- harness: `webcode-benchmark/evals.rag` grader: `openai/gpt-oss-120b (nebius)` commit: `5729a4e`
- groundedness = **0.4167** (+0.0000 vs prev), correctness = 0.4167, n = 12 (Exa published target ~79.4%)
- **why:** REAL github:exa-labs/benchmarks (commit 5729a4e) run against unbrowse via the real evals.rag harness; Nebius grader (gpt-oss-120b) not Exa gpt-5.4, so a real score on the real harness, not a same-grader head-to-head. Below Exa baseline  -  honest negative recorded.

## Self-improvement (reliable n>=30 A/B)

- baseline = 0.5 -> improved = 0.6 (**+0.100** at n>=30, margin>=0.05 = reliable)
- **honest negatives kept (3):**
  - NEGATIVE: dedup-by-domain (reverted)  -  hurt code-doc grounding (0.583->0.500)
  - NEGATIVE: ENRICH_CHARS 28k (reverted)  -  too much context diluted grounding (0.583->0.500)
  - NEGATIVE: query-aware windowing (reverted)  -  mid-page windows lost doc context (0.583->0.417)

## Discipline

- Single-run n=12 deltas are noise (~1-query variance); only n>=30 A/B with margin>=0.05 counts.
- A blocked service (DDG throttle / OpenRouter quota / missing key) is reported BLOCKED, never faked.
- Coverage is measured across tiers, never forced to pass.
