# Design-gap ledger — energy-ranking / contract / x402 (2026-06-20)

> AC1 seed. Each gap = (design ideal + source) vs (unbrowse reality at file:line) + severity + fill.
> Verdicts on G4/G5 confirmed by live-path trace. Moat note: fills WRAP, never expose, the signals.

## The ideal (from /lewis-strategy + aiko contract substrate)
`energy = -score`, `attention = softmax(-energy/T)`, ties → canonical order; the scalar is a fusion
of independent uncorrelated witnesses; selection only *settles* at a two-witness quorum else
escalate-to-parent (bounded ~7). aiko logs per call: `iterated {energy_a, energy_b, agree, cohered,
contract_ok}`, soft-gated. Refs: `lewis-brain/references/10-verse-energy-attention.md`,
`scripts/verse_rank.py:174-183`, `aiko-engine3/src/contract/coherence.zig:34,71-82`,
`src/contract_bind.zig:16-33,179-192`.

| # | Gap | Ideal (source) | Reality (file:line) | Sev | Fill (wrap, don't rewrite) |
|---|-----|----------------|---------------------|-----|------|
| **G1** | Flat RRF, not evidence-routed | fusion weight `w_lex` collapses toward the base with real evidence (`verse_rank.py:174-183`) | `rrfFuse` gives every list a fixed `1/(RRF_K+rank)` vote regardless of evidence (`discovery.ts:347`, `RRF_K=60`) | med | `energy.ts` wraps RRF with confidence-weighted fusion behind one `energyScore()` |
| **G2** | No directional coverage (JEPA) | score "does the route's output cover the intent", asymmetric (`contract_bind.zig:16-33`) | `rank.ts` uses symmetric BM25/overlap; no coverage axis | med | add a coverage witness to `energy.ts` (2nd, uncorrelated) |
| **G3** | No settlement / abstention | 2-witness quorum + bounded-7 → escalate-to-parent (`coherence.zig:34,71-82`) | `rrfFuse` always returns `.slice(0,k)`; no abstention when nothing covers intent. LIVE: resolve "search posts" → ok:200, 0 candidates, no escalate signal | **HIGH** | `settle.ts` `settleOrEscalate()` → settled\|escalate(browse) |
| **G4** | Resolve contract-shaped, not declared | resolve ≡ declared truth-claim, per-call `iterated` row (`contract_bind.zig:179-192`) | **CONFIRMED:** `/search/resolve` (`search.ts:347`) → only `discovery.ts`; zero `contract-ledger` imports; `declared` written only at `contract.ts:359` + `skill-contract-persist.ts:72` (publish path) | med | fire-and-forget `appendDeclared` in `search.ts:~393` via `schedule(c,…)`, authenticated resolves only |
| **G5** | api-key→wallet declared but UNIMPLEMENTED (the user's ask) | "pay one x402 challenge via wallet OR api-key (key wraps wallet)" | **CONFIRMED:** `KeyFunding {kind:"wallet"\|"credit"}` (`keys.ts:151-153`); execute settles **only** `credit` (`skills.ts:420`); a `kind:"wallet"` key falls through to 402 — `skills.ts:414-416` "Wallet-bound keys are NOT settled here". `setKeyFunding` binds the wallet (`agents.ts:175`) but nothing settles from it server-side. No unified `admitPayment`. | **HIGH** | add `funding.kind==="wallet"` branch at `skills.ts:417-429`: server-sign a Flex x402 authorization against the key's bound wallet (reuse `wallet-signer.ts` + facilitator), settle like `handleFlexPaymentAuthorized`. Optionally extract a unified `payment-admission.ts admitPayment()` boundary. |
| **G6** | No public docs | — | no doc explains energy-ranking or the x402 API + dual payment | med | `docs/concepts/energy-ranking.md` (WHAT) + `docs/api/x402.md` (endpoints + `X402PaymentRequirementV2` envelope + dual pay); anchor each claim; leak-guard |

## Priority (Dijkstra — settle the user's explicit asks first)
1. **G5** — api-key-wraps-wallet x402 settlement (explicit ask, HIGH) → witness: `dual-payment-admission.test.ts`
2. **G6** — docs (x402 API + energy explainer) (explicit ask) → witness: paper-gate + leak-guard
3. **G3** — settlement/abstention (HIGH, quality) → witness: resolve escalates instead of empty list
4. **G1/G2** — energy unification (the `energyScore` interface) → witness: `energy-interface.test.ts`
5. **G4** — declare-on-resolve (med) → witness: a declared row appears after an authed resolve

## Moat discipline
Every fill is an INTERFACE over existing signals. Docs name `energy`/`attention`/`settlement`/the
x402 envelope (the WHAT); never the fusion weights, coverage features, or capture engine (the HOW).
