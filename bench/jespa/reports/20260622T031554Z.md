# jespa-bench seed report — 2026-06-22T03:15:54Z

Three independent, isolated axes (firmament boundary 4 / plan node 5).
Seed run: the harness enumerated all three axes; each carries its own honest verdict.
No fabricated numbers — a missing axis is BLOCKED with the cause.

- bin under test: `bun src/cli.ts` (UNBROWSE_BIN unset → local source)

| axis | what | status | detail |
|---|---|---|---|
| a | web-agent NO-AUTH (public one-hole read) | PASS | public no-auth read reached target (marker echoed); bin=bun src/cli.ts |
| b | web-agent WITH-AUTH (credential reaches target) | PASS | delegated to capability/webagent/gate_auth.sh (rc=0); bin=bun src/cli.ts |
| c | INTERNAL benchmarks (jespa route-retrieval R@1) | FAIL | jespa route-retrieval R@1 vs keyword baseline, real corpus (rc=1) |

## Per-axis status lines
```
axis(a) web-agent NO-AUTH    : PASS
axis(b) web-agent WITH-AUTH  : PASS
axis(c) INTERNAL benchmarks  : FAIL
```

## How to grow this seed
- axis (a): widen from postman-echo to the cloned no-auth web-agent corpora
  (`bench/exa/vendor/benchmarks`, `bench/browsecomp`) scored per-task.
- axis (b): feed real cookies/credentials so `capability/webagent/gate_auth.sh`
  exits 0 (PASS) instead of BLOCKED; add WASP/AgentDojo authenticated tasks.
- axis (c): grow the .bench-gate corpus so n_eval >= 100 and the R@1 lift can settle;
  add `jespa-benchmarks-gate.sh` (the accumulating reproduced-win ledger).
