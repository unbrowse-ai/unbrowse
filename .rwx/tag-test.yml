on:
  github:
    push:
      if: ${{ event.git.tag != '' }}
      init:
        commit-sha: ${{ event.git.sha }}

base:
  image: ubuntu:24.04
  config: rwx/base 1.1.1

tasks:
  - key: code
    call: git/clone 2.0.8
    with:
      repository: https://github.com/unbrowse-ai/unbrowse-dev.git
      ref: ${{ init.commit-sha }}
      github-token: ${{ github.token }}
