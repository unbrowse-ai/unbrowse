## What's New
The npm CLI release path now publishes its Bun binary assets to the public `unbrowse-ai/unbrowse` GitHub release that installers and packaged fallback downloads already use.

## Fixes
Tag releases no longer upload CLI assets only to the private repo and then fail asset verification before npm publish.

## Performance
No runtime performance change in this patch release.
