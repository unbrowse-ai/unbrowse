## Fixes

- Resolve no longer returns phantom endpoints that can't be executed. Canonical replay endpoints now use deterministic IDs and are persisted to the skill cache.
- Stale vector index entries pointing to deleted endpoints are silently dropped instead of surfacing as ghost endpoints in resolve responses.
- New admin route `POST /v1/ops/purge-reindex` to rebuild the vector index from current DB state.
