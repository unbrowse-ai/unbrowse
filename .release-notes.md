## Fixes

- Server crash on fresh npm install -- missing agentmail and x402/fetch dependencies caused ERR_MODULE_NOT_FOUND on startup
- Resolve returning wrong or no endpoints after v3.6.0 -- stale vecdb endpoint IDs now recovered via URL-template matching instead of silently dropped
- Multi-broker default reduced to 1 to prevent stale tab registry

## What's New

- Post-release verification pipeline -- automated smoke tests run after every release on a staging VM with traction anomaly detection every 6 hours and auto-filed GitHub issues on regression
