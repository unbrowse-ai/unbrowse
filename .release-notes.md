## v11.3.3

### Bug Fixes
- **mcp**: stop packaged CLI `main()` from dumping help onto JSON-RPC stdout and exiting (hosts no longer drop MCP transport on first `get`)
- **ci**: run npm publish path on `ubuntu-latest` (self-hosted had 0 runners; 11.3.x tags had been cancelling after 24h)
- **ci (public)**: OIDC npm publish falls back to `NPM_TOKEN` when Trusted Publishing returns ENEEDAUTH

### Notes
- Monorepo + packaged stdio witnesses: `tests/mcp-packaged-get-transport.test.ts`, `tests/runtime-entry-kind.test.ts`
- Rebuild injects `UNBROWSE_RUNTIME_ENTRY` so single-file MCP bundles never auto-run CLI main
