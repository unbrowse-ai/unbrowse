## Fixes

- MCP server now loads cleanly in LM Studio and other strict MCP clients. The `unbrowse_annotate` tool declared its schema under the wrong field, which caused the entire tool list to be rejected with a Zod validation error on `tools[29].inputSchema`.
- Runtime entry resolution: the JS entrypoint is now wrapped as a `file://` URL and `isMainModule` unwraps it, fixing module-detection edge cases on packaged installs.
