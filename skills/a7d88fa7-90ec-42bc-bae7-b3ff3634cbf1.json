{
  "skill_id": "a7d88fa7-90ec-42bc-bae7-b3ff3634cbf1",
  "version": "1.0.0",
  "schema_version": "1",
  "lifecycle": "active",
  "execution_type": "http",
  "created_at": "2026-02-13T19:18:23.944Z",
  "updated_at": "2026-02-17T14:17:05.379Z",
  "name": "simmer-markets",
  "intent_signature": "Manage Polymarket positions, track user watchlists, and monitor wallet status through Simmer Markets. Access real-time market data, trading agent activity, and global trade history to analyze prediction market performance.",
  "domain": "simmer.markets",
  "description": "Manage Polymarket positions, track user watchlists, and monitor wallet status through Simmer Markets. Access real-time market data, trading agent activity, and global trade history to analyze prediction market performance.",
  "owner_type": "marketplace",
  "auth_type": "Bearer Token",
  "service_name": "simmer-markets",
  "endpoints": [
    {
      "endpoint_id": "HEAD--login",
      "method": "HEAD",
      "url_template": "https://simmer.markets/login",
      "headers_template": {},
      "query_template": {},
      "body_template": null,
      "idempotency": "safe",
      "verification_status": "pending",
      "reliability_score": 0.5,
      "description": "",
      "transform_code": "(data) => {\n  /*__unbrowse_auto_ssr__*/ \n  if (typeof data !== 'string') return data;\n  const t = data.trimStart().slice(0, 500).toLowerCase();\n  const looksHtml = t.startsWith('<!doctype') || t.startsWith('<html') || t.includes('<body') || t.includes('<head');\n  if (!looksHtml) return data;\n  return summarizeHtml(data);\n}"
    }
  ],
  "marketplace_meta": {
    "creator_wallet": "5RpPtxzTx7AGZqmG2Br3cXorhLjDKLxkUEeLhni1K6pt",
    "download_count": 0,
    "total_versions": 0
  },
  "_references": [
    "REFERENCE.md",
    "TRANSFORMS.json"
  ],
  "_has_scripts": [
    "api.ts"
  ],
  "_skill_md": "---\nname: simmer-markets\ndescription: Internal API for simmer-markets\nmetadata:\n  author: unbrowse\n  version: \"1.0\"\n  baseUrl: \"https://simmer.markets\"\n  authMethod: \"Bearer Token\"\n  endpointCount: 8\n  apiType: \"internal\"\n  category: \"web-app\"\n---\n\n# SimmerMarkets API\n\n> Reverse-engineered internal API. Unofficial — may change without notice.\n\n## Documentation Index\n- `SKILL.md` (this file)\n- `references/REFERENCE.md` (detailed endpoint docs)\n- `references/ENDPOINTS.json` (machine-readable endpoint catalog)\n- `scripts/api.ts` (typed client interface)\n\n## Authentication\nBearer Token — Authentication method detected from captured traffic.\n\n### Providing credentials\n- Prefer passing auth as request headers or cookies at call time (do not hardcode secrets into the skill).\n- If executing via an external proxy/executor, pass per-call credentials (cookies and/or headers) so they are not persisted.\n\n## Dependencies\n- Runtime capable of HTTPS requests.\n- If using `scripts/api.ts`: TypeScript toolchain (or transpile to JS).\n\n## Endpoints\n\n### Data Read (6)\n- `GET /api/global/agent-trades` — Get global agent trade history.\n- `GET /api/user/polymarket-positions` — Retrieve user's Polymarket positions.\n- `GET /api/user/polymarket-wallet/status` — Check user's Polymarket wallet status.\n- `GET /api/user/watchlist/ids` — Get IDs of markets in user's watchlist.\n- `GET /api/v0/sdk/{sdkId}/onramps` — Get available onramps for an SDK.\n- `GET /wallet-book/v1/stable/wallet-book.json` — Download the wallet book JSON.\n\n### Data Write (1)\n- `POST /api/v1/{v1Id}` — Execute a write operation via the v1 API.\n\n### Core API (1)\n- `GET /api/{apiId}` — Access a core API endpoint.\n\n## Quick Start\n\nHere are a few examples to get you started using the `simmer-markets` API:\n\n```typescript\n// Retrieve the user's Polymarket positions.  This is a common data-read operation.\nasync function getPositions() {\n  try {\n    const positions = await client.getUserPolymarketPositions();\n    console.log(\"User positions:\", positions);\n    return positions;\n  } catch (error) {\n    console.error(\"Failed to get positions:\", error);\n    return null;\n  }\n}\n```\n\n```typescript\n// Get global agent trade history.\nasync function getAgentTrades() {\n  try {\n    const trades = await client.getGlobalAgentTrades();\n    console.log(\"Agent trades:\", trades);\n    return trades;\n  } catch (error) {\n    console.error(\"Failed to get agent trades:\", error);\n    return null;\n  }\n}\n```\n\n```typescript\n// Execute a write operation via the v1 API.  Replace v1Id and payload with appropriate values.\nasync function executeWriteOperation(v1Id: string, payload: any) {\n  try {\n    const result = await client.postApiV1(v1Id, payload);\n    console.log(\"Write operation result:\", result);\n    return result;\n  } catch (error) {\n    console.error(\"Failed to execute write operation:\", error);\n    return null;\n  }\n}\n```\n\n## Additional Notes\n\n> Reverse-engineered internal API. Unofficial — may change without notice.\n\n## Documentation Index\n- `SKILL.md` (this file)\n- `references/REFERENCE.md` (detailed endpoint docs)\n- `references/ENDPOINTS.json` (machine-readable endpoint catalog)\n- `scripts/api.ts` (typed client interface)\n\n## Authentication\nBearer Token — Authentication method detected from captured traffic.\n\n### Providing credentials\n- Prefer passing auth as request headers or cookies at call time (do not hardcode secrets into the skill).\n- If executing via an external proxy/executor, pass per-call credentials (cookies and/or headers) so they are not persisted.\n\n## Dependencies\n- Runtime capable of HTTPS requests.\n- If using `scripts/api.ts`: TypeScript toolchain (or transpile to JS).\n\n## Endpoints\n\n### Data Read (6)\n\n\n### Data Write (1)\n\n\n### Core API (1)\n\n\n## Quick Start\n\nHere's a Quick Start to get you interacting with the simmer-markets API:\n\n```typescript\n// Fetch the user's Polymarket positions.  Useful for determining current portfolio state.\nasync function getPolymarketPositions() {\n  const positions = await client.get_api_user_polymarket_positions();\n  console.log(\"Polymarket Positions:\", positions);\n  return positions;\n}\n```\n\n```typescript\n// Retrieve trades made by other agents.  Useful for market analysis.\nasync function getAgentTrades() {\n  const trades = await client.get_api_global_agent_trades();\n  console.log(\"Agent Trades:\", trades);\n  return trades;\n}\n```\n\n```typescript\n// Write data to a v1 API endpoint (example - replace with actual data and v1Id).\nasync function writeDataToV1(v1Id: string, data: any) {\n  try {\n    const response = await client.post_api_v1_$v1Id({ v1Id }, data);\n    console.log(\"Write Response:\", response);\n    return response;\n  } catch (error) {\n    console.error(\"Error writing data:\", error);\n    throw error;\n  }\n}\n```\n\n## Additional Notes\n\n**Type:** Reverse-engineered internal API (unofficial)\n**Auth:** Bearer Token\n**Base URL:** `https://simmer.markets`\n**Captured Auth:** 5 auth headers, 98 session cookies\n\n## About This Skill\n\nThis skill provides access to Simmer Markets's internal API — the hidden endpoints that power their web/mobile app.\nThese are NOT official public APIs. They were captured by observing network traffic while using the site.\n\n**Important:**\n- Auth tokens in `auth.json` may expire — re-capture if you get 401 errors\n- Internal APIs can change without notice — endpoints may break\n- Rate limits are unknown — be conservative with request frequency\n\n## When to Use This Skill\n\nUse this skill when you need to:\n- Access Simmer Markets data without official API access\n- Automate actions on simmer.markets (faster than browser automation)\n- Call the same endpoints the Simmer Markets frontend uses\n\n## Quick Start\n\n```typescript\nimport { SimmerMarketsClient } from \"./scripts/api.ts\";\n\n// Load captured auth from auth.json\nconst client = await SimmerMarketsClient.fromAuthFile(\"auth.json\");\n\n// Make requests to internal endpoints\nconst result = await client.get(\"/endpoint\");\n```\n\n## Captured Authentication\n\nThe `auth.json` file contains credentials extracted from browser traffic:\n- **Auth headers:** 5 (authorization, sec-fetch-storage-access, x-magic-api-key, x-dyn-api-version, x-dyn-session-public-key)\n- **Session cookies:** 98\n\nIf auth expires, re-run `unbrowse_login` or `unbrowse_capture` to refresh tokens.\n\n## Internal Endpoints (14)\n\n\n## Error Handling\n\n```typescript\ntry {\n  const data = await client.get(\"/resource\");\n} catch (err) {\n  if (err.message.includes(\"401\")) {\n    // Auth expired — need to re-capture\n    console.error(\"Auth expired, re-run unbrowse_login\");\n  } else {\n    console.error(\"API error:\", err.message);\n  }\n}\n```\n\nSee [REFERENCE.md](references/REFERENCE.md) for detailed endpoint documentation.\n\n## Endpoints (Auto-generated)\n\nSee [REFERENCE.md](references/REFERENCE.md) for detailed endpoint documentation.\n"
}