{
  "skill_id": "02fd0fcc-9747-484c-9ea0-d4c4fd129e73",
  "version": "1.0.0",
  "schema_version": "1",
  "lifecycle": "active",
  "execution_type": "http",
  "created_at": "2026-02-13T17:17:01.793Z",
  "updated_at": "2026-02-17T14:17:05.379Z",
  "name": "apply-blue-badge-service-gov-uk",
  "intent_signature": "Manage Blue Badge applications on the apply-blue-badge.service.gov.uk portal by retrieving applicant details and processing application information. Streamline the process of viewing and interacting with parking permit requests through secure session-based access.",
  "domain": "apply-blue-badge.service.gov.uk",
  "description": "Manage Blue Badge applications on the apply-blue-badge.service.gov.uk portal by retrieving applicant details and processing application information. Streamline the process of viewing and interacting with parking permit requests through secure session-based access.",
  "owner_type": "marketplace",
  "auth_type": "Cookie-based (session)",
  "service_name": "apply-blue-badge-service-gov-uk",
  "endpoints": [
    {
      "endpoint_id": "GET--applicant",
      "method": "GET",
      "url_template": "https://apply-blue-badge.service.gov.uk/applicant",
      "headers_template": {},
      "query_template": {},
      "body_template": null,
      "idempotency": "safe",
      "verification_status": "pending",
      "reliability_score": 0.5,
      "description": "",
      "transform_code": "(data) => {\n  /*__unbrowse_auto_ssr__*/ \n  if (typeof data !== 'string') return data;\n  const t = data.trimStart().slice(0, 500).toLowerCase();\n  const looksHtml = t.startsWith('<!doctype') || t.startsWith('<html') || t.includes('<body') || t.includes('<head');\n  if (!looksHtml) return data;\n  return summarizeHtml(data);\n}"
    }
  ],
  "marketplace_meta": {
    "creator_wallet": "13iUZgRtXDE7LYPC37TMys4FAWAPv7qAdLjjDQgD3tgb",
    "download_count": 0,
    "total_versions": 1,
    "latest_version_hash": "49d4d873344a2604c4a20471b32cc78252e46dd1dc3fbf799692754453abf3bd"
  },
  "_references": [
    "REFERENCE.md",
    "TRANSFORMS.json"
  ],
  "_has_scripts": [
    "api.ts"
  ],
  "_skill_md": "---\nname: apply-blue-badge-service-gov-uk\ndescription: Internal API for apply-blue-badge-service-gov-uk\nmetadata:\n  author: unbrowse\n  version: \"1.0\"\n  baseUrl: \"https://apply-blue-badge.service.gov.uk\"\n  authMethod: \"Cookie-based (session)\"\n  endpointCount: 1\n  apiType: \"internal\"\n  category: \"web-app\"\n---\n\n# ApplyBlueBadgeServiceGovUk API\n\n> Reverse-engineered internal API. Unofficial — may change without notice.\n\n## Documentation Index\n- `SKILL.md` (this file)\n- `references/REFERENCE.md` (detailed endpoint docs)\n- `references/ENDPOINTS.json` (machine-readable endpoint catalog)\n- `scripts/api.ts` (typed client interface)\n\n## Authentication\nCookie-based (session) — Authentication method detected from captured traffic.\n\n### Providing credentials\n- Prefer passing auth as request headers or cookies at call time (do not hardcode secrets into the skill).\n- If executing via an external proxy/executor, pass per-call credentials (cookies and/or headers) so they are not persisted.\n\n## Dependencies\n- Runtime capable of HTTPS requests.\n- If using `scripts/api.ts`: TypeScript toolchain (or transpile to JS).\n\n## Endpoints\n\n### Data Read (1)\n- `GET /applicant` — Retrieve applicant data, potentially paginated for updates.\n\n## Quick Start\n\nHere's the Quick Start section for the `apply-blue-badge-service-gov-uk` API skill document:\n\n```typescript\n// Retrieve applicant data (first page)\nasync function getFirstPageOfApplicants() {\n  const applicants = await client.get_applicant();\n  console.log(\"First applicant:\", applicants[0]);\n  return applicants;\n}\n```\n\n```typescript\n// Retrieve applicant data (all pages, assuming a 'nextPageToken' is returned)\nasync function getAllApplicants() {\n  let allApplicants = [];\n  let nextPageToken = null;\n\n  do {\n    const response = await client.get_applicant({ pageToken: nextPageToken });\n    allApplicants = allApplicants.concat(response.data);\n    nextPageToken = response.nextPageToken;\n  } while (nextPageToken);\n\n  console.log(\"Total applicants:\", allApplicants.length);\n  return allApplicants;\n}\n```\n\n## Additional Notes\n\n> Reverse-engineered internal API. Unofficial — may change without notice.\n\n## Documentation Index\n- `SKILL.md` (this file)\n- `references/REFERENCE.md` (detailed endpoint docs)\n- `references/ENDPOINTS.json` (machine-readable endpoint catalog)\n- `scripts/api.ts` (typed client interface)\n\n## Authentication\nCookie-based (session) — Authentication method detected from captured traffic.\n\n### Providing credentials\n- Prefer passing auth as request headers or cookies at call time (do not hardcode secrets into the skill).\n- If executing via an external proxy/executor, pass per-call credentials (cookies and/or headers) so they are not persisted.\n\n## Dependencies\n- Runtime capable of HTTPS requests.\n- If using `scripts/api.ts`: TypeScript toolchain (or transpile to JS).\n\n## Endpoints\n\n### Data Read (1)\n\n\n## Quick Start\n\nHere's a quick start to using the `apply-blue-badge-service-gov-uk` API. These examples demonstrate common read operations.\n\n```typescript\n// Retrieve applicant data.  Handle potential pagination.\nasync function getFirstApplicantName(): Promise<string | null> {\n  try {\n    const response = await client.getApplicant();\n    if (response.applicants && response.applicants.length > 0) {\n      return response.applicants[0].name;\n    } else {\n      return null; // No applicants found\n    }\n  } catch (error) {\n    console.error(\"Error fetching applicant data:\", error);\n    return null;\n  }\n}\n```\n\n```typescript\n// Retrieve all applicant data, handling pagination.\nasync function getAllApplicants(): Promise<Applicant[]> {\n  let allApplicants: Applicant[] = [];\n  let page = 1;\n  let hasNextPage = true;\n\n  while (hasNextPage) {\n    try {\n      const response = await client.getApplicant({ page });\n      if (response.applicants) {\n        allApplicants = allApplicants.concat(response.applicants);\n      }\n      hasNextPage = response.hasNextPage || false; // Assuming the API returns a 'hasNextPage' boolean\n      page++;\n    } catch (error) {\n      console.error(\"Error fetching applicant data:\", error);\n      return allApplicants; // Return what we have so far\n    }\n  }\n\n  return allApplicants;\n}\n```\n\n## Additional Notes\n\n> Reverse-engineered internal API. Unofficial — may change without notice.\n\n## Documentation Index\n- `SKILL.md` (this file)\n- `references/REFERENCE.md` (detailed endpoint docs)\n- `references/ENDPOINTS.json` (machine-readable endpoint catalog)\n- `scripts/api.ts` (typed client interface)\n\n## Authentication\nCookie-based (session) — Authentication method detected from captured traffic.\n\n### Providing credentials\n- Prefer passing auth as request headers or cookies at call time (do not hardcode secrets into the skill).\n- If executing via an external proxy/executor, pass per-call credentials (cookies and/or headers) so they are not persisted.\n\n## Dependencies\n- Runtime capable of HTTPS requests.\n- If using `scripts/api.ts`: TypeScript toolchain (or transpile to JS).\n\n## Endpoints\n\n### Data Read (1)\n\n\n## Quick Start\n\nHere's a quick start to using the `apply-blue-badge-service-gov-uk` API. These examples demonstrate common read operations.\n\n```typescript\n// Retrieve applicant data.  Handles potential pagination.\nasync function getApplicantData() {\n  let allApplicants = [];\n  let page = 1;\n  let hasNextPage = true;\n\n  while (hasNextPage) {\n    const response = await client.getApplicant({ page });\n    if (response.data) {\n      allApplicants = allApplicants.concat(response.data);\n    }\n\n    if (response.next_page) {\n      page = response.next_page;\n    } else {\n      hasNextPage = false;\n    }\n  }\n  return allApplicants;\n}\n```\n\n## Additional Notes\n\n> Reverse-engineered internal API. Unofficial — may change without notice.\n\n## Documentation Index\n- `SKILL.md` (this file)\n- `references/REFERENCE.md` (detailed endpoint docs)\n- `references/ENDPOINTS.json` (machine-readable endpoint catalog)\n- `scripts/api.ts` (typed client interface)\n\n## Authentication\nCookie-based (session) — Authentication method detected from captured traffic.\n\n### Providing credentials\n- Prefer passing auth as request headers or cookies at call time (do not hardcode secrets into the skill).\n- If executing via an external proxy/executor, pass per-call credentials (cookies and/or headers) so they are not persisted.\n\n## Dependencies\n- Runtime capable of HTTPS requests.\n- If using `scripts/api.ts`: TypeScript toolchain (or transpile to JS).\n\n## Endpoints\n\n### Data Read (1)\n\n\n## Quick Start\n\nHere are a few examples to get you started:\n\n```typescript\n// Retrieve the first page of applicant data.\nconst firstPage = await client.get_applicant();\nconsole.log(firstPage.data);\n```\n\n```typescript\n// Retrieve applicant data with pagination.\nconst page2 = await client.get_applicant({ page: 2, pageSize: 10 });\nconsole.log(page2.data);\n```\n\n## Additional Notes\n\n> Reverse-engineered internal API. Unofficial — may change without notice.\n\n## Documentation Index\n- `SKILL.md` (this file)\n- `references/REFERENCE.md` (detailed endpoint docs)\n- `references/ENDPOINTS.json` (machine-readable endpoint catalog)\n- `scripts/api.ts` (typed client interface)\n\n## Authentication\nCookie-based (session) — Authentication method detected from captured traffic.\n\n### Providing credentials\n- Prefer passing auth as request headers or cookies at call time (do not hardcode secrets into the skill).\n- If executing via an external proxy/executor, pass per-call credentials (cookies and/or headers) so they are not persisted.\n\n## Dependencies\n- Runtime capable of HTTPS requests.\n- If using `scripts/api.ts`: TypeScript toolchain (or transpile to JS).\n\n## Endpoints\n\n### Data Read (1)\n\n\n## Quick Start\n\nHere are a few examples to get you started:\n\n```typescript\n// Retrieve the first page of applicant data.\nconst firstPage = await client.get_applicant();\nconsole.log(firstPage.data);\n```\n\n```typescript\n// Retrieve all applicant data by iterating through pages.\nlet allApplicants = [];\nlet nextPage = await client.get_applicant();\nwhile (nextPage.data.length > 0) {\n  allApplicants = allApplicants.concat(nextPage.data);\n  if (nextPage.next) {\n    nextPage = await client.get_applicant({ page: nextPage.next });\n  } else {\n    break;\n  }\n}\nconsole.log(`Retrieved ${allApplicants.length} applicants.`);\n```\n\n## Additional Notes\n\n> Reverse-engineered internal API. Unofficial — may change without notice.\n\n## Documentation Index\n- `SKILL.md` (this file)\n- `references/REFERENCE.md` (detailed endpoint docs)\n- `references/ENDPOINTS.json` (machine-readable endpoint catalog)\n- `scripts/api.ts` (typed client interface)\n\n## Authentication\nCookie-based (session) — Authentication method detected from captured traffic.\n\n### Providing credentials\n- Prefer passing auth as request headers or cookies at call time (do not hardcode secrets into the skill).\n- If executing via an external proxy/executor, pass per-call credentials (cookies and/or headers) so they are not persisted.\n\n## Dependencies\n- Runtime capable of HTTPS requests.\n- If using `scripts/api.ts`: TypeScript toolchain (or transpile to JS).\n\n## Endpoints\n\n### Data Read (1)\n\n\n## Quick Start\n\nHere's how to quickly get started using the `apply-blue-badge-service-gov-uk` API.\n\nRetrieve applicant data:\n\n```typescript\n// Get the first page of applicants.\nasync function getFirstPageOfApplicants() {\n  const applicants = await client.get_applicant();\n  console.log(applicants);\n  return applicants;\n}\n```\n\nRetrieve all applicant data using pagination:\n\n```typescript\n// Retrieve all applicants by iterating through paginated results.\nasync function getAllApplicants() {\n  let allApplicants = [];\n  let next = null;\n  do {\n    const response = await client.get_applicant({next});\n    allApplicants = allApplicants.concat(response.data);\n    next = response.next;\n  } while (next);\n  return allApplicants;\n}\n```\n\n## Additional Notes\n\n**Type:** Reverse-engineered internal API (unofficial)\n**Auth:** Cookie-based (session)\n**Base URL:** `https://apply-blue-badge.service.gov.uk`\n**Captured Auth:** 5 session cookies\n\n## About This Skill\n\nThis skill provides access to Apply Blue Badge Service Gov Uk's internal API — the hidden endpoints that power their web/mobile app.\nThese are NOT official public APIs. They were captured by observing network traffic while using the site.\n\n**Important:**\n- Auth tokens in `auth.json` may expire — re-capture if you get 401 errors\n- Internal APIs can change without notice — endpoints may break\n- Rate limits are unknown — be conservative with request frequency\n\n## When to Use This Skill\n\nUse this skill when you need to:\n- Access Apply Blue Badge Service Gov Uk data without official API access\n- Automate actions on apply-blue-badge.service.gov.uk (faster than browser automation)\n- Call the same endpoints the Apply Blue Badge Service Gov Uk frontend uses\n\n## Quick Start\n\n```typescript\nimport { ApplyBlueBadgeServiceGovUkClient } from \"./scripts/api.ts\";\n\n// Load captured auth from auth.json\nconst client = await ApplyBlueBadgeServiceGovUkClient.fromAuthFile(\"auth.json\");\n\n// Make requests to internal endpoints\nconst result = await client.get(\"/endpoint\");\n```\n\n## Captured Authentication\n\nThe `auth.json` file contains credentials extracted from browser traffic:\n- **Auth headers:** 0 (none)\n- **Session cookies:** 5\n\nIf auth expires, re-run `unbrowse_login` or `unbrowse_capture` to refresh tokens.\n\n## Internal Endpoints (1)\n\n\n## Error Handling\n\n```typescript\ntry {\n  const data = await client.get(\"/resource\");\n} catch (err) {\n  if (err.message.includes(\"401\")) {\n    // Auth expired — need to re-capture\n    console.error(\"Auth expired, re-run unbrowse_login\");\n  } else {\n    console.error(\"API error:\", err.message);\n  }\n}\n```\n\nSee [REFERENCE.md](references/REFERENCE.md) for detailed endpoint documentation.\n\n## Endpoints (Auto-generated)\n\nSee [REFERENCE.md](references/REFERENCE.md) for detailed endpoint documentation.\n\n## Endpoints (Auto-generated)\n\nSee [REFERENCE.md](references/REFERENCE.md) for detailed endpoint documentation.\n\n## Endpoints (Auto-generated)\n\nSee [REFERENCE.md](references/REFERENCE.md) for detailed endpoint documentation.\n\n## Endpoints (Auto-generated)\n\nSee [REFERENCE.md](references/REFERENCE.md) for detailed endpoint documentation.\n\n## Endpoints (Auto-generated)\n\nSee [REFERENCE.md](references/REFERENCE.md) for detailed endpoint documentation.\n\n## Endpoints (Auto-generated)\n\n<!-- UNBROWSE:ENDPOINTS:BEGIN -->\n- `GET /applicant`\n<!-- UNBROWSE:ENDPOINTS:END -->\n\n<!-- UNBROWSE:ENDPOINT_DOCS:BEGIN -->\n### `GET /applicant`\n- Status: `pending` | Quality: `0` | Health: `100`\n\n#### Input\n- Path params: none\n- Query: none\n- Body schema: none\n\n#### Output\n- Output schema: (learned from executions)\n<!-- UNBROWSE:ENDPOINT_DOCS:END -->\n"
}