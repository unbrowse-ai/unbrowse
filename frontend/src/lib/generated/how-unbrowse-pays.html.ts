// Auto-generated from docs/HOW_UNBROWSE_PAYS.md by frontend/scripts/codegen-docs.mjs.
// Inlined to bypass runtime fs reads in opennextjs-cloudflare worker bundling.
// To refresh: node frontend/scripts/codegen-docs.mjs
export const HOW_UNBROWSE_PAYS_HTML: string = "<h2>What this document is</h2>\n<p>This page explains how money moves through Unbrowse on a paid call: what is free,\nwhat costs, how a charge is split, and who signs for the wallet. Every claim cites\na file and line in the codebase so the behaviour can be verified, not just trusted.</p>\n<h2>Free vs paid</h2>\n<p>Discovery and internal-API routing are free. When an agent asks Unbrowse to resolve\nan intent, search cached endpoints, or read a route graph, nothing is charged.</p>\n<p>Paid execution is the only billed step. When a workflow runs a captured route on\nyour behalf, that single call settles over x402 in USDC. There is no subscription\nand no monthly plan: you pay per request, and only for the requests that execute.</p>\n<h2>The split</h2>\n<p>Every paid call is divided by Faremeter Flex into a fixed, on-chain split. The\nsettlement roles are defined in <code>backend/src/services/flex.ts:39</code>, and the split\npercentages are set by named basis-point constants:</p>\n<ul>\n<li>Platform: 50% (<code>PLATFORM_BPS = 5000</code>, see <code>backend/src/services/flex.ts:68</code>).</li>\n<li>Indexer / contributor pool: 35%, shared across the contributors who captured and\nmaintain the route, weighted by their cumulative contribution.</li>\n<li>Domain owner: 15% (<code>OWNER_BPS = 1500</code>, see <code>backend/src/services/flex.ts:87</code>),\ncarved off the top when a verified owner wallet is bound to the domain.</li>\n</ul>\n<p>So a paid call with a bound domain owner settles as a <strong>50/35/15</strong> split: half to\nthe platform that runs the infrastructure, just over a third to the indexers who\ndiscovered the route, and the rest to the website owner. When no owner wallet is\nbound, the owner share folds back into the contributor pool.</p>\n<h2>Who signs the wallet</h2>\n<p>Unbrowse owns the payment intent: what is being paid for, how much, and to which\nrecipient token account. It does not own the wallet. Wallet ownership, session\nlifecycle, and the sign and broadcast pipeline are delegated to an agent wallet.</p>\n<p>The compatible and tested agent wallet is <code>lobster.cash</code>. Fund a <code>lobster.cash</code>\nwallet once, and it pays each x402 challenge automatically. Unbrowse never creates\nwallets and never asks for private keys, seed phrases, or raw card details.</p>\n<h2>Verify it yourself</h2>\n<p>The split constants above are read straight from the code. Run\n<code>bash scripts/lobster-compat-gate.sh</code> to confirm Unbrowse's live x402 challenge\nmeets the wallet requirements (Solana settlement, USDC currency). The gate exits 0\nwhen the contract holds, which is the runnable proof behind this page.</p>\n";
