// Auto-generated from docs/HOW_UNBROWSE_PAYS.md by frontend/scripts/codegen-docs.mjs.
// Inlined to bypass runtime fs reads in opennextjs-cloudflare worker bundling.
// To refresh: node frontend/scripts/codegen-docs.mjs
export const HOW_UNBROWSE_PAYS_HTML: string = "<h2>What this document is</h2>\n<p>This page explains how money moves through unbrowse on every paid call. The math, the splits, the wallet ownership, and the rails. Every claim cites a file and line in the codebase.</p>\n<h2>Web2 subscription path</h2>\n<p>For users who never want to touch crypto, unbrowse exposes a Stripe-backed subscription that hides the on-chain settlement rail entirely:</p>\n<ol>\n<li>The user opens the URL returned by <code>POST /v1/account/billing-subscribe-url</code> (<code>backend/src/routes/account.ts</code>), pays with a card via Stripe Checkout, and lands back on the configured success URL.</li>\n<li>Stripe's webhook (<code>POST /v1/billing/webhook</code>) drives <code>syncStripeDataToUserKV</code> (<code>backend/src/services/stripe.ts:226</code>), which writes the canonical <code>STRIPE_SUB_CACHE</code> to <code>stripe:customer:&lt;customer_id&gt;</code> in KV.</li>\n<li>On every paid <code>execute</code>, when the worker is started with <code>UNBROWSE_BILLING_ENABLED=1</code> the sponsor middleware (<code>backend/src/middleware/sponsor.ts:maybeSponsor</code>) consults <code>subscriptionAdmits</code> BEFORE the wallet-pay platform-sponsor check. A subscribed user with quota is admitted; usage is recorded against the Stripe-tracked balance via <code>recordUsage</code>; a sponsor-ledger row tagged <code>payment_method: &quot;stripe&quot;</code> is written for audit. The agent never sees a 402.</li>\n<li>Status is observable via <code>GET /v1/account/billing-status</code> — <code>{ subscription_active, plan_name, monthly_limit_usd, consumed_usd, remaining_usd, renewal_date, payment_method_present, auto_refill_enabled }</code>. The absent-subscription case returns <code>{ subscription_active: false, ...nulls }</code>, never 404.</li>\n<li>The user manages cards, invoices, and cancellation via the Stripe customer-portal URL from <code>POST /v1/account/billing-portal-url</code>.</li>\n</ol>\n<p>The gate is reversible: <code>UNBROWSE_BILLING_ENABLED</code> unset → the Stripe lane is a no-op and every paid call rides the wallet path documented below. On a worker where <code>STRIPE_SECRET_KEY</code> is also unset, the three account routes soft-fail with <code>503 billing_not_configured</code> rather than crash.</p>\n<p>Non-subscribers (or subscribers over quota with no auto-refill) continue to use the wallet + Flex + sponsor rails described next.</p>\n<h2>The 50/35/15 split</h2>\n<p>Every paid <code>unbrowse execute</code> settles on-chain through a Faremeter Flex authorization with up to five recipients in one transaction.</p>\n<ul>\n<li><strong>Platform: 50%.</strong> <code>PLATFORM_BPS = 5000</code> at <code>backend/src/services/flex.ts:39</code>. This share funds the substrate (resolve, marketplace, facilitator). Can be overridden per deployment via the <code>FLEX_PLATFORM_BPS</code> env var, range 0 to 10000.</li>\n<li><strong>Site owner: 15%.</strong> <code>OWNER_BPS = 1500</code> (added beside <code>PLATFORM_BPS</code> in <code>backend/src/services/flex.ts</code>). Routed only when the domain has been DNS-claimed and the skill carries <code>owner_compensation_opt_in === true</code> (<code>backend/src/types.ts:437</code>) together with a verified <code>owner_wallet_usdc_ata</code>.</li>\n<li><strong>Indexer pool: 35%.</strong> The remaining 3500 bps when a site owner is bound. Split across <code>skill.contributors[]</code> weighted by <code>cumulative_delta</code> (<code>backend/src/services/flex.ts:65-74</code>), then deduped with <code>mergeSplits</code> (<code>backend/src/services/flex.ts:98-113</code>) because the Flex on-chain program rejects duplicate recipients.</li>\n<li><strong>No site owner claimed: 50% platform, 50% indexer pool.</strong> When <code>owner_compensation_opt_in</code> is false or no DNS claim exists, the carve falls through. <code>contributorPool = 10000 - PLATFORM_BPS = 5000</code> bps (<code>backend/src/services/flex.ts:66</code>).</li>\n</ul>\n<p>The split is capped at <code>FLEX_MAX_SPLITS = 5</code> (<code>backend/src/services/flex.ts:40</code>). The top four contributors by <code>cumulative_delta</code> take the contributor pool when unclaimed; the top three when a site-owner lane is active.</p>\n<h2>Per-skill markup (5 to 80 percent)</h2>\n<p>Every <code>SkillManifest</code> carries an optional <code>markup_bps</code> field (Pontus / ABK Labs brief 2026-05-21: &quot;5-80% markup potential on Flex&quot;). When set, it overrides the default <code>PLATFORM_BPS = 5000</code> for that specific skill's settlements. The clamp is enforced server-side at <code>backend/src/services/flex.ts:48-49</code> (<code>MARKUP_BPS_MIN = 500</code>, <code>MARKUP_BPS_MAX = 8000</code>); values outside the range snap to the nearest bound.</p>\n<p>What the agent sees: <code>markup_bps</code> shifts the platform's bps share, NOT the owner or indexer math. The owner lane stays at <code>OWNER_BPS = 1500</code> when DNS-claimed; the contributor pool absorbs the residual.</p>\n<p>Concrete examples (owner claimed, default OWNER_BPS=1500):</p>\n<ul>\n<li><code>markup_bps = 500</code> (the floor, 5% platform) -&gt; 5% platform, 15% owner, 80% indexers.</li>\n<li><code>markup_bps = 5000</code> (the unset default, 50% platform) -&gt; 50% platform, 15% owner, 35% indexers. This is the canonical case above.</li>\n<li><code>markup_bps = 8000</code> (the ceiling, 80% platform) -&gt; 80% platform, 15% owner, 5% indexers.</li>\n</ul>\n<p>When no owner is claimed, the residual flows entirely into the contributor pool, so a <code>markup_bps = 500</code> skill gives indexers 95% of every paid call. A premium-content skill that wants to fund the platform aggressively can dial up to 8000.</p>\n<p>The field is set at publish time (<code>PublishSkillInput.markup_bps</code> on the SDK and CLI) and is per-skill, not per-call. Skills with no value continue to settle at the documented 50/35/15.</p>\n<h2>Indexer earnings</h2>\n<p>When you use unbrowse to call a website nobody has indexed yet, the act of resolving and executing CAPTURES the underlying API. You become the indexer of that skill, recorded as <code>indexer_id</code> on the <code>SkillManifest</code> (<code>backend/src/types.ts:409</code>).</p>\n<p>From that point forward, every paid <code>execute</code> against the same skill, by any agent, routes you a share. Your wallet address lives on the <code>SkillContributor</code> row that the publish handler attaches (<code>backend/src/types.ts:516-531</code>). At settlement, <code>computeFlexSplits</code> reads <code>skill.contributors</code>, filters entries with a non-empty <code>wallet_address</code>, weights by <code>cumulative_delta</code>, and produces the Flex split array (<code>backend/src/services/flex.ts:54-87</code>).</p>\n<p>Payouts land in the same on-chain transaction as the platform's. There is no escrow, no nightly batch, no opt-in to be paid.</p>\n<h2>Site owner earnings</h2>\n<p>If you run the domain that unbrowse is indexing, you can claim 15% of every paid call to that domain's skills.</p>\n<p>The claim is a DNS-TXT record. The verifier resolves <code>_unbrowse-claim.&lt;apex&gt;</code> through two independent DoH providers (Cloudflare and Google), both must return a TXT whose value is <code>unbrowse-claim=&lt;challenge&gt;;wallet=&lt;your-wallet&gt;</code>. The contract is described in <code>.claude/firmament-step2.md</code> lines 86-176 and implemented under <code>backend/src/routes/claim.ts</code> and <code>backend/src/services/domain-claim.ts</code>.</p>\n<p>The skill must also carry <code>owner_compensation_opt_in === true</code> (<code>backend/src/types.ts:437</code>). The publish handler sets this when the indexer or owner explicitly opts in. The OWNER_BPS lane fires only when both conditions hold: the opt-in flag is true AND <code>owner_wallet_usdc_ata</code> resolves from the <code>domain-wallet:&lt;domain&gt;</code> KV binding.</p>\n<p>See <code>docs/CLAIM_YOUR_DOMAIN.md</code> for the step-by-step.</p>\n<h2>Payment provider choice (Pay signer / lobster.cash / Privy / external)</h2>\n<p>The substrate never holds private keys; you bind a signer at <code>unbrowse setup</code>. As of 2026-05-21 there are four supported providers selectable from the CLI prompt and the <code>/account</code> web page (<code>POST /v1/account/payment-provider</code> persists the choice; <code>backend/src/services/flex.ts</code> honours it on dispatch).</p>\n<ul>\n<li><strong>Pay signer</strong> -- TouchID-backed signer, USDC settlement via the wallet-pay rail / search_catalog. The thinnest path for laptop agents; the wallet lives in the macOS keychain. The CLI bridge is <code>src/payments/paysh-pay.ts</code> (shells to <code>pay curl &lt;url&gt;</code> on each settle).</li>\n<li><strong>lobster.cash</strong> -- Crossmint-backed credit-card -&gt; virtual-card -&gt; Solana funnel. The recommended path for non-technical users; subscription billing tops up the wallet automatically. <code>npx @crossmint/lobster-cli setup</code> provisions the account.</li>\n<li><strong>Privy embedded</strong> -- Solana wallet provisioned in-browser via Privy. Bound to the user's <code>agent.wallet_address</code> after <code>verifyPrivyAuthToken</code> succeeds (<code>backend/src/services/privy.ts</code>). The right answer when the agent runs as a web app and the user signs in with email or OAuth instead of installing a CLI.</li>\n<li><strong>External wallet</strong> -- bring your own Solana signer. The substrate emits a payment envelope; any wallet that can sign a Faremeter Flex authorization works.</li>\n</ul>\n<p>The choice is reversible: <code>unbrowse setup</code> re-runs the prompt, or POST a new provider to <code>/v1/account/payment-provider</code>. The selected provider gates the runtime dispatch path; the on-chain split math (above) is identical across providers.</p>\n<h2>Wallets stay with lobster.cash</h2>\n<p>The substrate never holds private keys. The frontend recommends lobster.cash as the payout wallet (the live <code>/how-unbrowse-pays</code> page renders from <code>docs/HOW_UNBROWSE_PAYS.md</code> via <code>frontend/src/lib/docs-renderer.ts</code>): <code>npx @crossmint/lobster-cli setup</code> provisions a Solana account, lobster signs, unbrowse only declares intent, amount, recipient, and memo. If you want a different signer, any wallet that can sign a Faremeter Flex authorization works.</p>\n<h2>Wallet payments are the main rail</h2>\n<p>Settlement runs on HTTP-native micropayments (the canonical <a href=\"https://www.x402.org\">x402</a> protocol — see appendix for implementers). unbrowse rotates between two facilitators per request:</p>\n<ul>\n<li><strong>Flex</strong> (default). Self-hosted Faremeter facilitator with native splits. The 50/35/15 math above runs on Flex. Selection logic at <code>backend/src/services/rail-rotation.ts</code>.</li>\n<li><strong>PayAI exact</strong> (rotation fallback). Single-recipient. Does NOT carry splits, so when PayAI wins the rotation the contributor and owner pools settle off-rail (deferred until a Flex-rail transaction). <code>PAYAI_ROTATION_BPS</code> env (default 5000) controls the weight; the hash of <code>agent_id</code> decides which rail's accept entry comes first in the 402 envelope.</li>\n</ul>\n<p>Stripe optionally wraps either rail for fiat-billed customers, but the underlying on-chain split is unchanged.</p>\n<h2>No account required to pay</h2>\n<p>Agents pay per call. The 402 response carries the price, recipient, and memo. The caller signs and the facilitator settles. There is no signup, no API key gate on the pay path.</p>\n<p>Accounts exist for one reason: to accumulate and read earnings. The magic-link flow at <code>backend/src/routes/auth.ts:53-172</code> issues an API key and an agent_id; the agent_id is what we attribute contributions to. You can use unbrowse without ever creating one; you just can't see a balance until you do.</p>\n<h2>Settlement cycle</h2>\n<p>Each paid execute writes one row to <code>sponsor:ledger:&lt;id&gt;</code> carrying the agent, the skill, <code>amount_uc</code> (µ¢), <code>creator_wallet</code>, and <code>settled_at</code>. Those rows are the source-of-truth for the dashboards (<code>GET /v1/analytics/payments</code> reads them directly: platform cut, sponsor recoup, creator payouts are all derived from the ledger, not stamped separately).</p>\n<p>Operators roll the unsettled rows into a batch via two admin routes:</p>\n<ol>\n<li><code>POST /v1/admin/aggregate-settlement?since=&amp;until=&amp;dry_run=</code> — walks <code>sponsor:ledger:*</code>, filters to rows whose <code>batch_settled_tx</code> is absent, groups them by <code>skill_id</code>, looks up each <code>SkillManifest</code>, runs <code>computeFlexSplits</code> to derive the recipient layout (platform / owner / contributor), and writes a <code>settlement:ledger:&lt;batch_id&gt;</code> row in <code>status:&quot;pending&quot;</code>. <code>dry_run=1</code> still persists the batch — it is the source-of-truth that <code>execute-settlement</code> reads from — but lets the operator inspect the recipients before submitting.</li>\n<li><code>POST /v1/admin/execute-settlement</code> body <code>{batch_id, dry_run?}</code> — reads the batch, normalises the per-recipient µ¢ amounts into Flex bps (re-normalised to sum to exactly 10000), assembles the on-chain authorization, and either returns it for inspection (<code>dry_run:true</code>) or signs and submits it; each source row is stamped with <code>batch_settled_tx + batch_settled_at</code> so it does not replay.</li>\n</ol>\n<p>Batch state is readable any time via <code>GET /v1/admin/settlement/:batch_id</code> — returns the pending or executed <code>SettlementBatch</code> row from KV, or 404 when absent. The shape includes <code>recipients[]</code> (with the <code>owner_lane</code> flag set on the verified-domain entry), <code>tx_signature</code> (once executed), <code>total_amount_uc</code>, and <code>source_ledger_ids[]</code>.</p>\n<h3>Domain opt-out propagation</h3>\n<p>A verified domain owner can opt out via the DNS-TXT takedown flow (<code>unbrowse-takedown=&lt;challenge&gt;</code>). The verify endpoint writes a persistent <code>domain-optout:&lt;domain&gt;</code> record to KV. Aggregation reads that key for every skill it groups; when present, it coerces <code>owner_compensation_opt_in</code> to <code>false</code> at compute time and the 1500 bps owner lane rolls back into the contributor + platform pool. Opt-out is a binary, one-way signal — the owner can re-enable compensation by re-running the claim flow.</p>\n<h2>What this is not</h2>\n<ul>\n<li>Not a custodial wallet. Funds never sit in an unbrowse account.</li>\n<li>Not a marketplace fee on capture. We charge on execute, not on publish.</li>\n<li>Not a subscription. There is no monthly plan; every paid call is its own settlement.</li>\n<li>Not a cross-chain bridge. Solana USDC only; the on-chain mint is hardcoded at <code>backend/src/services/flex.ts:44</code>.</li>\n</ul>\n";
