## Summary
- The execute handler called saveTrace() locally but never called recordExecution() to POST to the backend /v1/stats/execution endpoint
- Backend had no execution telemetry — agent profiles showed zero executions from CLI users
- Added fire-and-forget recordExecution() calls after both the primary execution path and the 404-recovery path

## Changes
- src/api/routes.ts: import recordExecution from client, add fire-and-forget calls after executeSkill and after 404-recovery resolveAndExecute

Fixes #104

🤖 Generated with [Claude Code](https://claude.com/claude-code)
