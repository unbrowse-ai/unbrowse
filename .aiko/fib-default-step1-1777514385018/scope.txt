Step 1 no-op gate fired twice because teachings lines lack proper dw: and ab: fields matching the gate's schema requirements. Previous turns DID make deepwiki and agent-browser calls but the gate rejected the resulting teachings lines. The fix is to make fresh deepwiki and agent-browser calls THIS turn and produce a teachings line that exactly matches the required schema with dw:<owner/repo#topic> and ab:<evidence> fields.
